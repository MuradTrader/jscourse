````markdown
# 1. Сценарий интервью

## Контекст:

Интервьюер сначала задал вопрос:

«Что такое анонимная функция?»
**Ответ кандидата:** «Функция без имени.»
(Эта часть была простой.)

Далее интервьюер спросил:

«Что такое функции первого класса в JavaScript?»
Кандидат запаниковал, так как слышал про колбэки, функции высшего порядка,
стрелочные функции и т.д., но термин «функции первого класса» был ему незнаком.
Кандидат признался, что не знает.

Наконец, интервьюер задал вопрос о разнице между функциональным выражением
и функциональным оператором (function statement).
Кандидат перепутал их, ошибочно ответив, что функция-оператор – это просто функция с именем.

## Основной вывод:

Многие разработчики путают или не до конца понимают эти термины.
Это видео (и данные заметки) призваны их прояснить.

---

# 2. Анонимные функции

## Определение:

Анонимная функция — это функция без имени.

## Ключевые моменты:

- В JavaScript, когда вы создаёте функцию без имени, она считается анонимной.

### Пример:

```javascript
// Анонимная функция, присвоенная переменной:
const greet = function () {
  console.log("Hello, World!");
};

greet(); // Выведет: "Hello, World!"
```
````

## Важно:

Хотя создавать анонимные функции для использования в качестве значений вполне разрешено,
спецификация ECMAScript требует, чтобы операторы-функции (функциональные декларации) имели имя.
Если попытаться написать оператор-функцию без имени, это вызовет синтаксическую ошибку.

**Вот так не передав как значение:** `function() {...}`

---

# 3. Функции первого класса

## Определение:

Язык обладает функциями первого класса (или функции считаются гражданами первого класса),
если функции обрабатываются как любые другие значения.

## Следствия:

Функции можно:

- Присваивать переменным.
- Передавать в качестве аргументов другим функциям.
- Возвращать из других функций.
- Хранить в структурах данных (массивах, объектах и т.д.).

### Пример – присваивание и передача функции:

```javascript
// Присвоение функции переменной (функциональное выражение)
const add = function (x, y) {
  return x + y;
};

// Передача функции как аргумента (колбэк)
function process(a, b, callback) {
  console.log("Result:", callback(a, b));
}

process(2, 3, add); // Выведет: "Result: 5"
```

## Ключевая идея:

В JavaScript функции являются функциями первого класса,
то есть вы можете использовать их так же, как числа, строки или объекты.

---

# 4. Функциональный оператор (декларация) vs. функциональное выражение

## A. Функциональный оператор (декларация)

### Определение:

Функциональный оператор (функциональная декларация) определяется с использованием
ключевого слова `function`, за которым следует имя функции.

### Синтаксис:

```javascript
// Функциональная декларация
function sayHello() {
  console.log("Hello!");
}

// Её можно вызвать до места объявления, так как декларации поднимаются (hoisted).
sayHello(); // Выведет: "Hello!"
```

### Поведение hoisting:

Функциональные декларации поднимаются в начало области видимости,
поэтому определение функции доступно даже если вызов происходит
до её фактического размещения в коде.

## B. Функциональное выражение

### Определение:

Функциональное выражение — это когда функция определяется как часть выражения,
например, присваивается переменной.

### Синтаксис:

```javascript
// Функциональное выражение (анонимное)
const greet = function () {
  console.log("Greetings!");
};

greet(); // Выведет: "Greetings!"
```

### Поведение hoisting:

При функциональных выражениях поднимается только объявление переменной
(инициализированное значением `undefined`, если используется `var`);
само значение функции присваивается только при выполнении строки кода.

### Пример:

```javascript
console.log(add); // Если 'add' объявлена через var, выводится 'undefined'
var add = function (x, y) {
  return x + y;
};
// Вызов add() до её определения приведёт к ошибке.
```

## Ключевое отличие:

**Основная разница в hoisting:**

- Функциональные декларации поднимаются со своим полным определением.
- Функциональные выражения поднимаются как переменные,
  но их значение (функция) присваивается только во время выполнения.

---

# 5. Именованное функциональное выражение

## Определение:

Именованное функциональное выражение похоже
на обычное функциональное выражение, но функция получает имя.

### Синтаксис:

```javascript
const factorial = function fact(n) {
  if (n <= 1) return 1;
  return n * fact(n - 1);
};

console.log(factorial(5)); // Выведет: 120
```

## Ключевые моменты:

- Имя функции (в данном случае `fact`) доступно только внутри самой функции (полезно для рекурсии).
- Вне функции вы обращаетесь к ней через переменную (здесь `factorial`).

## Особый случай (подводный камень):

Если попытаться вызвать `fact` из внешней области,
возникнет `ReferenceError`, так как оно недоступно вне функции.

```javascript
// console.log(fact(5)); // ReferenceError: fact is not defined
```

---

# 6. Параметры vs. Аргументы

## Параметры:

Это идентификаторы, указанные в определении функции.
**Пример:** В функции `add(a, b) { ... }` переменные `a` и `b` являются параметрами.

## Аргументы:

Это реальные значения, передаваемые функции при её вызове.
**Пример:** При вызове `add(2, 3)`, числа 2 и 3 — это аргументы.

## Ключевой момент:

Не путайте эти термины — они означают разные вещи.

---

# 7. Функции первого класса: повторное рассмотрение

## Резюме:

Поскольку функции в JavaScript являются функциями первого класса,
их можно присваивать переменным, передавать как аргументы и возвращать из других функций.

Эта возможность позволяет реализовывать:

- **Колбэки:** Функции, передаваемые как аргументы.
- **Функции высшего порядка:** Функции, которые принимают другие функции в качестве параметров или возвращают их.
- **Стрелочные функции:** Лаконичный синтаксис для написания функций (они следуют тем же правилам области видимости).

### Пример – возвращение функции (связь с замыканиями):

```javascript
function multiplier(factor) {
  return function (number) {
    return number * factor;
  };
}

const double = multiplier(2);
console.log(double(5)); // Выведет: 10
```

Здесь функция `multiplier` возвращает внутреннюю функцию,
которая «запоминает» значение `factor` благодаря замыканию.

---

# 8. Стрелочные функции (краткое упоминание)

## Замечание:

Спикер отмечает, что стрелочные функции были введены в ES6 и работают аналогично обычным функциям
с точки зрения области видимости и возможности быть функциями первого класса.

### Пример:

```javascript
const add = (a, b) => a + b;
console.log(add(3, 4)); // Выведет: 7
```

## Ключевой момент:

Стрелочные функции — это просто более лаконичный синтаксис,
который подчиняется тем же принципам, что и функциональные выражения,
в отношении их статуса функций первого класса.

---

# 9. Финальные советы для интервью

## При объяснении анонимных функций:

Укажите, что это функции без имени, часто используемые как значения.

## При объяснении функций первого класса:

Скажите, что в JavaScript функции могут использоваться как любые другие значения
— их можно присваивать переменным, передавать и возвращать.

**Можно сказать:**
«Функция первого класса — это просто функция, которая рассматривается как значение.»

## При объяснении разницы между функциональными операторами (декларациями) и функциональными выражениями:

Объясните, что функциональные декларации поднимаются со своими определениями,
а функциональные выражения не поднимаются полностью,
так как их значение присваивается во время выполнения.

## Дополнительно:

Поясните, что функциональное выражение может быть как анонимным, так и именованным,
но его присвоение происходит во время выполнения.

### Анонимная функция:

Часто используется в функциональных выражениях:

```javascript
const anon = function () {
  console.log("I have no name!");
};
```

### Именованное функциональное выражение:

Позволяет задать внутреннее имя для рекурсии или отладки:

```javascript
const named = function myFunc() {
  console.log("I have a name inside!");
};
```

---

## Итог

- **Анонимные функции** — это функции без имени, используемые как значения.
- **Функции первого класса** означают, что функции могут использоваться как любые другие значения.
- **Функциональные декларации** поднимаются со своими определениями,
  а **функциональные выражения** поднимаются как переменные,
  но их значение становится доступным только во время выполнения.
- **Именованные функциональные выражения** позволяют задать внутреннее имя для рекурсии,
  которое не доступно за пределами функции.
- **Параметры** — это переменные, указанные в определении функции,
  а **аргументы** — реальные значения, передаваемые при вызове.

Это знание поможет вам уверенно отвечать на вопросы на собеседованиях и писать гибкий, современный JavaScript.

---

# 🎯 Рекурсия: Понять, как работает рекурсия

## Цель:

Представь, что у нас есть задача — посчитать от какого-то числа до 1.
Например, если число 5, нужно вывести:

```
5
4
3
2
1
```

## ✅ Как это сделать через рекурсию?

Вот простой пример на JavaScript:

```javascript
function countDown(n) {
  if (n === 0) return; // База: если дошли до 0 — стоп
  console.log(n); // Печатаем текущее число
  countDown(n - 1); // Рекурсивный вызов (уменьшаем число на 1)
}

countDown(5); // Запустим с 5
```

## 🧠 Что здесь происходит:

Мы вызываем `countDown(5)`:

- Печатает 5
- Вызывает `countDown(4)`

`countDown(4)`:

- Печатает 4
- Вызывает `countDown(3)`

И так далее...
Когда `n === 0`, функция ничего не делает и заканчивается.

## 🔑 Важно: Базовый случай

`if (n === 0) return;`
Этот момент останавливает бесконечный вызов самой себя.
Если не сделать "стоп", рекурсия вызовет себя бесконечно и будет ошибка (stack overflow).

## 📊 Как работает пошагово:

| Вызов функции | Что выводится | Что происходит дальше        |
| ------------- | ------------- | ---------------------------- |
| countDown(5)  | 5             | вызывает countDown(4)        |
| countDown(4)  | 4             | вызывает countDown(3)        |
| countDown(3)  | 3             | вызывает countDown(2)        |
| countDown(2)  | 2             | вызывает countDown(1)        |
| countDown(1)  | 1             | вызывает countDown(0) (стоп) |
| countDown(0)  | -             | заканчивает работу           |

## ✅ Результат:

```
5
4
3
2
1
```

---

# 🔥 Факториал через рекурсию

## ✅ Напомним, что такое факториал:

Факториал числа n (обозначается n!) — это произведение всех чисел от n до 1.

**Например:**
`5! = 5 × 4 × 3 × 2 × 1 = 120`

## 📜 Вот пример функции факториала на JavaScript:

```javascript
function factorial(n) {
  if (n === 1) return 1; // Базовый случай (остановка рекурсии)
  return n * factorial(n - 1); // Рекурсивный вызов
}

console.log(factorial(5)); // 120
```

## 🧠 Что происходит пошагово?

### 1. Первый вызов:

`factorial(5):`
`5 * factorial(4)` // Окей, сначала нужно узнать факториал 4

### 2. Второй вызов:

`factorial(4):`
`4 * factorial(3)` // Окей, надо узнать факториал 3

### 3. Третий вызов:

`factorial(3):`
`3 * factorial(2)` // Окей, надо узнать факториал 2

### 4. Четвёртый вызов:

`factorial(2):`
`2 * factorial(1)` // Окей, надо узнать факториал 1

### 5. Пятый вызов (СТОП!):

`factorial(1):`
`return 1` // БАЗА! Возвращаем 1 назад

## 🔁 Теперь "разматываем обратно":

- `factorial(2) = 2 * 1 = 2`
- `factorial(3) = 3 * 2 = 6`
- `factorial(4) = 4 * 6 = 24`
- `factorial(5) = 5 * 24 = 120`

## ✅ Финальный ответ: 120

## 📊 Таблица шагов:

| Вызов        | Что возвращает |
| ------------ | -------------- |
| factorial(1) | 1              |
| factorial(2) | 2 × 1 = 2      |
| factorial(3) | 3 × 2 = 6      |
| factorial(4) | 4 × 6 = 24     |
| factorial(5) | 5 × 24 = 120   |

## ⚙️ Почему работает?

- Функция вызывает саму себя с меньшим числом.
- Когда доходит до 1 — остановка (база).
- После этого начинает возвращать значения обратно, перемножая их.

## ⚡ Полная картина:

```
factorial(5) -> 5 * factorial(4)
              -> 5 * (4 * factorial(3))
              -> 5 * (4 * (3 * factorial(2)))
              -> 5 * (4 * (3 * (2 * factorial(1))))
              -> 5 * (4 * (3 * (2 * 1))) = 120
```

---

## ✅ Как рекурсия "разматывается":

- **factorial(1)** → Это базовый случай. Возвращает 1.  
  `factorial(1) = 1`

- **factorial(2)** → Берём результат factorial(1) (который 1), умножаем на 2:  
  `factorial(2) = 2 * 1 = 2`

- **factorial(3)** → Берём результат factorial(2) (который 2), умножаем на 3:  
  `factorial(3) = 3 * 2 = 6`

- **factorial(4)** → Берём результат factorial(3) (который 6), умножаем на 4:  
  `factorial(4) = 4 * 6 = 24`

- **factorial(5)** → Берём результат factorial(4) (который 24), умножаем на 5:  
  `factorial(5) = 5 * 24 = 120`

💥 **И финальный ответ: 120**

```

```
