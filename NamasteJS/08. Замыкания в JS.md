````markdown
# 1. Введение в замыкания

## Энтузиазм спикера:

Спикер с воодушевлением говорит о замыканиях и обещает,
что к концу видео вы полностью поймёте этот механизм.

## Что вы узнаете:

- Простой пример замыкания
- Как функция «запоминает» своё лексическое окружение
- Более продвинутые применения (например, возвращение функции и связанные с этим нюансы)

---

# 2. Простой пример замыкания

## Пример кода 1: Простое замыкание

```javascript
function x() {
  let a = 7; // Переменная 'a' определена в области функции x
  function y() {
    console.log(a); // Функция y получает доступ к 'a' из родительской области (x)
  }
  y(); // Вызов внутренней функции y внутри x
}

x(); // Вызов x; выводит: 7
```
````

## Объяснение:

- Функция `x` создаёт переменную `a` со значением 7.
- Внутри `x` определена функция `y`, которая выводит значение `a`.
- Благодаря лексическому окружению `y` имеет доступ к `a`, даже если сама `a` не объявлена внутри `y`.
- Совокупность функции `y` и её доступа к переменным `x` образует замыкание.

## Ключевой момент:

Замыкание — это просто функция, объединённая со своим лексическим окружением
(то есть со всеми переменными, которые были доступны во время её создания).

---

# 3. Расширенный пример: возвращение функции

## Пример кода 2: Возвращение внутренней функции

```javascript
function x() {
  let a = 7; // Переменная 'a' находится в области функции x
  function y() {
    console.log(a); // Функция y использует переменную 'a' из области x
  }
  return y; // Возвращаем внутреннюю функцию y вместе с её лексическим окружением
}

let z = x(); // Теперь z содержит функцию y
z(); // Вызов z выводит: 7
```

## Объяснение:

Здесь функция `x` возвращает внутреннюю функцию `y`.
Даже после завершения выполнения `x` и удаления её контекста из стека вызовов,
возвращённая функция `y` продолжает «помнить» своё лексическое окружение – в частности, переменную `a` со значением 7.
При последующем вызове `z()` выводится 7,
потому что `y` по-прежнему ссылается на переменную `a` из области `x`.

## Важная деталь:

Внутренняя функция не захватывает статическую копию значения `a`; она сохраняет ссылку.
Если значение `a` изменится до вызова замыкания, функция отобразит новое значение.

### Пример:

```javascript
function x() {
  let a = 7;
  function y() {
    console.log(a);
  }
  a = 100; // Изменяем значение 'a' перед возвратом функции y
  return y;
}
let z = x();
z(); // Выведет: 100, так как y ссылается на переменную 'a', которая теперь равна 100
```

---

# 4. Что такое замыкание?

## Определение:

Замыкание – это комбинация функции и лексического окружения,
в котором эта функция была объявлена.

## Проще говоря:

Это не только код функции, но и сама функция вместе со всеми локальными переменными,
которые были доступны в момент её создания.

## Почему это важно:

Замыкания позволяют функциям получать доступ к переменным из внешней функции
даже после того, как внешняя функция завершила своё выполнение.
Это мощная возможность, которая позволяет реализовывать следующие паттерны:

- Инкапсуляция данных (паттерн модуля)
- Создание приватных переменных
- Каррирование (currying)
- Реализация функций, выполняющихся только один раз (например, функция «once»)
- И многое другое

## По определению MDN:

Замыкание — это функция,
объединённая с её окружающим состоянием (лексическим окружением).

---

# 5. Практическое применение замыканий

## Пример использования 1: Сохранение состояния (счётчик)

```javascript
function makeCounter() {
  let count = 0; // Приватная переменная
  return function () {
    count++;
    console.log(count);
  };
}

const counter = makeCounter();
counter(); // 1
counter(); // 2
counter(); // 3
```

### Объяснение:

Внутренняя функция, возвращаемая из `makeCounter()`, сохраняет ссылку на переменную `count`.
Каждый вызов этой функции обновляет и выводит текущее значение счётчика,
даже если функция `makeCounter()` уже завершила своё выполнение.

## Пример использования 2: Паттерн модуля

```javascript
const myModule = (function () {
  let privateVar = "I am private";
  return {
    getPrivate: function () {
      return privateVar;
    },
    setPrivate: function (newValue) {
      privateVar = newValue;
    },
  };
})();

console.log(myModule.getPrivate()); // "I am private"
myModule.setPrivate("new value");
console.log(myModule.getPrivate()); // "new value"
```

### Объяснение:

Паттерн модуля использует замыкания для инкапсуляции приватных переменных
и предоставляет доступ к ним только через возвращённые функции.
Переменная `privateVar` остаётся доступной только через методы `getPrivate` и `setPrivate`, а не напрямую снаружи.

---

# 6. Распространённые подводные камни и советы для интервью

## Помните:

Замыкание сохраняет ссылку на переменные родительской области, а не их статическую копию.

## Совет для интервью:

При вопросе «Что такое замыкание?» можно ответить:
«Замыкание — это функция вместе с её лексическим окружением.
Это означает, что функция имеет доступ к переменным,
которые были в области видимости во время её создания,
даже если внешняя функция уже завершила выполнение.»

## Пример «подводного камня»:

```javascript
function outer() {
  let a = 7;
  function inner() {
    console.log(a);
  }
  a = 100;
  return inner;
}
const closureFunc = outer();
closureFunc(); // Выведет: 100, а не 7!
```

## Другие применения в JavaScript:

Замыкания являются неотъемлемой частью асинхронного программирования
(например, при использовании `setTimeout` в циклах).
Они применяются в обработчиках событий, итераторах и многих продвинутых паттернах.

---

# 7. Резюме и заключительные мысли

## Ключевые моменты:

- **Определение замыкания:** Функция, объединённая с её лексическим окружением.
- **Базовый пример:** Внутренняя функция получает доступ к переменной (например, `a`) из внешней функции.
- **Расширенный пример:** Возвращение внутренней функции, которая сохраняет доступ к исходной области видимости,
  даже после завершения внешней функции.
- **Практическое применение:** Управление состоянием (счётчики), паттерн модуля, каррирование и другие техники.
- **Совет для интервью:** Говорите, что замыкание позволяет функции «запомнить» контекст её создания,
  что является фундаментальным для многих мощных паттернов в JavaScript.

## Помните:

Замыкания — одна из ключевых особенностей JavaScript, делающая функции гибкими и мощными.
Даже если вы явно не пишете замыкание, многие функции,
которые вы создаёте или используете, работают как замыкания «под капотом».

---

# 8. Дополнительный пример: Замыкания с setTimeout

Это классический вопрос на собеседованиях, где замыкания помогают
сохранить правильное значение переменной в цикле:

```javascript
for (var i = 1; i <= 3; i++) {
  setTimeout(function () {
    console.log("Value of i: " + i);
  }, i * 1000);
}
// Без замыкания все таймауты выведут "Value of i: 4"

// Чтобы исправить это, можно использовать let (блочную область видимости) или создать IIFE:
for (var i = 1; i <= 3; i++) {
  (function (j) {
    setTimeout(function () {
      console.log("Value of i: " + j);
    }, j * 1000);
  })(i);
}
```

## Объяснение:

Этот пример демонстрирует, как работают замыкания в JavaScript,
особенно при использовании асинхронных функций, таких как `setTimeout`.

### Проблема с использованием `var`

В первом фрагменте кода:

```javascript
for (var i = 1; i <= 3; i++) {
  setTimeout(function () {
    console.log("Value of i: " + i);
  }, i * 1000);
}
// Все таймауты выведут "Value of i: 4"
```

#### Цикл с переменной var

При использовании `var` переменная `i` имеет функциональную область видимости, а не блочную.
Это означает, что одна и та же переменная `i` используется во всех итерациях цикла.

#### Асинхронность setTimeout

Функции, переданные в `setTimeout`, выполняются асинхронно, спустя заданное время (в данном случае, 1, 2 и 3 секунды).
Однако к тому времени, когда эти функции начнут выполняться, цикл уже завершится,
и переменная `i` будет равна 4 (так как цикл останавливается, когда `i` становится 4).

#### Результат

Все отложенные функции обращаются к одной и той же переменной `i`, которая к моменту их вызова равна 4.
Поэтому в консоли мы увидим три раза строку "Value of i: 4".

### Решение с использованием замыкания (IIFE)

Во втором фрагменте кода используется немедленно вызываемое функциональное выражение (IIFE):

```javascript
for (var i = 1; i <= 3; i++) {
  (function (j) {
    setTimeout(function () {
      console.log("Value of i: " + j);
    }, j * 1000);
  })(i);
}
```

#### Немедленно вызываемая функция

Здесь для каждой итерации цикла сразу же вызывается функция, которая принимает аргумент `j`.
При вызове мы передаём текущее значение `i` в эту функцию.

#### Локальная переменная j

Переменная `j` объявляется как параметр функции и имеет собственную область видимости,
отличную от переменной `i` из внешнего цикла.
Таким образом, для каждой итерации создаётся своя копия значения.

#### Замыкание внутри setTimeout

Функция, переданная в `setTimeout`, «замыкает» значение `j`,
то есть сохраняет его в своём окружении даже после завершения выполнения IIFE.
Поэтому, когда срабатывает `setTimeout`, она выводит сохранённое значение `j`.

#### Результат

Теперь каждая функция выведет корректное значение:
через 1 секунду — 1, через 2 секунды — 2, и через 3 секунды — 3.

### Альтернативное решение с использованием let:

Если использовать переменную, объявленную с помощью `let`,
можно обойтись без IIFE, так как `let` имеет блочную область видимости:

```javascript
for (let i = 1; i <= 3; i++) {
  setTimeout(function () {
    console.log("Value of i: " + i);
  }, i * 1000);
}
```

В этом случае для каждой итерации создаётся новая копия переменной `i`,
и замыкание сохраняет нужное значение.

## Итог

- **Проблема:** При использовании `var` все функции `setTimeout` ссылаются на одну переменную,
  значение которой меняется после завершения цикла.
- **Решение:** Использование IIFE (или `let`) позволяет сохранить значение переменной
  для каждой итерации, создавая замыкание с нужным значением.

---

## 🔑 ИТОГО (если кратко):

| Особенность:                  | var (глобальный/функциональный контекст):   | let (блочный контекст):                |
| ----------------------------- | ------------------------------------------- | -------------------------------------- |
| Где живёт переменная?         | В общем контексте (глобальном или функции)  | В отдельном блочном контексте          |
| Одна переменная или много?    | Одна переменная i на все итерации           | Новая переменная i для каждой итерации |
| Что видит функция setTimeout? | Ссылается на одну общую i, которая станет 4 | Видит "свою" копию i                   |
| Что будет выведено?           | 4, 4, 4                                     | 1, 2, 3                                |

```

```
