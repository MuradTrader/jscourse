````markdown
# 1. Вступление и зацепка

"А ты вообще знаешь, как работает JavaScript и как исполняется код?"

👉 **Смысл**: Автор сразу задаёт вопрос, чтобы ты задумался —
"А чё, разве там что-то сложнее, чем просто написать и запустить код?"
И да, под капотом у JavaScript есть куча интересного, не всё так просто.

"JavaScript синхронный или асинхронный?"

📌 **Тут сравниваются два подхода**:

- **Синхронный** — код выполняется строго по порядку, одна строка за другой.
- **Асинхронный** — можно начать делать что-то сейчас,
  а закончить потом (например, дождаться ответа с сервера, не останавливая остальной код).

"JS однопоточный или многопоточный?"

📌 **Термины**:

- **Однопоточный** (single-threaded) — в один момент времени выполняется только одна задача.
- **Многопоточный** (multi-threaded) — можно делать много задач одновременно.

---

# 2. Главное ядро: Контекст Выполнения (Execution Context)

"Всё в JavaScript происходит внутри execution context."

⚙️ **Execution context** — это как большая коробка, в которой живёт весь твой код и всё, что ему нужно.
📦 Там лежат все переменные, функции, и именно там выполняется сам код.

👉 Спикер прям повторяет это много раз, чтобы вбить в голову:
"Всё, что ты пишешь, крутится в execution context."

---

# 3. Из чего состоит Execution Context?

## A. Память (Memory Component, aka Variable Environment)

"Первая часть — это память."

🧠 Тут хранятся все переменные и функции в виде пар "ключ-значение".

**Пример**:

```javascript
let a = 10;
```
````

В памяти будет так:

```json
{
  "a": 10
}
```

Также это официально зовётся **Variable Environment** — но можно говорить просто "память" для простоты.

## B. Код (Code Component, aka Thread of Execution)

"Вторая часть — код."

💻 Это та часть, где твой код реально выполняется построчно, сверху вниз.

📜 **"Thread of execution"** — можно представить как одну дорогу, по которой машина (интерпретатор) едет и читает твой код.

---

# 4. Почему говорят, что JavaScript однопоточный и синхронный?

"JavaScript — это однопоточный, синхронный язык."

- **Синхронный**: Все команды идут строго одна за другой.
- **Однопоточный**: Выполняется только одна задача в момент времени.

👉 То есть пока одна строка не выполнится — следующая не начнётся. Всё идёт по порядку.

---

# 5. А как же асинхронность (AJAX и т.д.)?

"Вы наверное подумали: 'А как же AJAX?'"

Да, есть штуки типа AJAX (отправить запрос и не ждать его ответа прямо сейчас).

"А" в AJAX значит "Asynchronous" (асинхронный) — но это не значит, что движок JS стал многопоточным.

🔥 **Важно**: Сам движок JS всегда однопоточный, а вот механизмы асинхронности
(например, setTimeout, промисы, async/await) — это как костыли вокруг него,
которые делают "вид", что что-то выполняется параллельно.
На самом деле — движок переключается между задачами по очереди, через очередь событий (event loop).

---

# 6. Быстро повторим, что узнали

- **Execution Context** — та самая коробка, где весь код живёт.
- Состоит из:
  - **Памяти** (Variable Environment): где переменные и функции.
  - **Кода** (Thread of Execution): где сам код исполняется построчно.
- **JavaScript** — однопоточный и синхронный язык.
- **Асинхронность** (AJAX, fetch и т.п.) — это не про движок JS, а про механизмы вокруг него.

---

## 💡 Главное, что нужно запомнить:

- Вся магия JavaScript происходит внутри **execution context**.
- Всё делается по очереди (синхронно), и только одна задача в момент времени (однопоточно).
- Асинхронные штуки типа AJAX, setTimeout — это не ломает однопоточность,
  а просто хитрый способ не тормозить выполнение кода.

---

## 📚 Термины:

- **Execution Context** — Контекст выполнения  
  👉 Это "среда", в которой исполняется твой JavaScript код.  
  Можно сказать проще — "коробка, где работает твой код".

- **Variable Environment** — Среда переменных (или Переменная среда)  
  👉 Это "место", где хранятся все переменные и функции, которые ты объявил.  
  По-простому: "память, куда кладутся переменные".

- **Thread of execution** — Поток выполнения  
  👉 Это тот самый "путь", по которому JavaScript
  "шагает" по коду — выполняет инструкции построчно, сверху вниз.  
  Можно сказать: "очередь исполнения команд".

Если перевести более "разговорно-программистски", получится так:

- **Execution Context** — "Коробка, в которой живёт и работает твой код".
- **Variable Environment** — "Память, где лежат все твои переменные и функции".
- **Thread of execution** — "Поток выполнения" или "дорожка, по которой код бежит строчка за строчкой".

```

```
