```markdown
# 🧠 Подробные заметки о Контексте Выполнения, Фазах и Стеке Вызовов

## 📌 1. Что происходит, когда вы запускаете JavaScript-программу?

👉 Когда вы запускаете JS-программу, за кулисами автоматически создаётся Контекст Выполнения.
Его можно представить как контейнер/коробку, в которой работает ваш код.

Всё в JavaScript выполняется внутри Контекста Выполнения.
Без Контекста Выполнения ничего не может запуститься.

## 📌 2. Что такое Контекст Выполнения?

👉 Он состоит из двух основных компонентов:
```

Контекст Выполнения = {

1. Компонент памяти (Variable Environment) => хранит переменные и функции,
2. Компонент кода (Thread of Execution) => выполняет код построчно.
   }

````

## 📌 3. Как создаётся Контекст Выполнения?
👉 В две фазы:
1. Фаза создания памяти,
2. Фаза выполнения кода.

### ✅ Пошаговое объяснение на примере кода:
```javascript
1 var n = 2;
2 function square(num) {
3    var ans = num * num;
4    return ans;
5 }
6 var square2 = square(n);
7 var square4 = square(4);
````

## 📌 4. Фаза 1: Фаза создания памяти (Hoisting)

👉 JS сканирует код построчно и выделяет память для переменных и функций.

| Идентификатор | Значение в памяти (изначально) |
| ------------- | ------------------------------ |
| n             | undefined                      |
| square        | весь код функции               |
| square2       | undefined                      |
| square4       | undefined                      |

**Примечание:**

- Переменные получают значение `undefined`
- Функции полностью сохраняются (их код «поднимается» вверх, hoisting)

📌 После выделения памяти, состояние выглядит так:

```javascript
n = undefined;
square = function (num) {
  var ans = num * num;
  return ans;
};
square2 = undefined;
square4 = undefined;
```

## 📌 5. Фаза 2: Фаза выполнения кода (выполнение происходит построчно)

👉 Теперь JS выполняет код построчно и обновляет значения:

**Строка 1:** `n = 2;`

```javascript
n = 2; // заменяет undefined
```

**Строка 6:** `var square2 = square(n);`
Здесь начинается вызов функции: `square(2)`

⚙️ Для вызова `square(2)` создаётся новый Контекст Выполнения функции
внутри Глобального Контекста Выполнения!

## 📌 6. Контекст Выполнения функции (square(2))

Опять же, две фазы:

### ✅ Фаза 1: Создание памяти внутри функции

| Идентификатор | Значение в памяти (изначально) |
| ------------- | ------------------------------ |
| num           | undefined                      |
| ans           | undefined                      |

### ✅ Фаза 2: Выполнение кода внутри функции

**Шаг 1:** Переменной num присваивается аргумент 2

```javascript
num = 2;
```

**Шаг 2:** Вычисление: `ans = num * num;`

```javascript
ans = 2 * 2 = 4;
```

**Шаг 3:** `return ans;` ⇒ возвращает 4

После возврата управление возвращается к строке 6,
и переменная square2 становится равной 4.

### 🔑 На данный момент:

| Идентификатор | Значение    |
| ------------- | ----------- |
| n             | 2           |
| square        | код функции |
| square2       | 4           |
| square4       | undefined   |

**Строка 7:** `var square4 = square(4);`
Функция вызывается снова: `square(4)`
⚙️ Для этого вызова снова создаётся новый Контекст Выполнения

## 📌 7. Контекст Выполнения функции (square(4))

### ✅ Фаза 1: Создание памяти внутри функции

| Идентификатор | Значение в памяти (изначально) |
| ------------- | ------------------------------ |
| num           | undefined                      |
| ans           | undefined                      |

### ✅ Фаза 2: Выполнение кода внутри функции

**Шаг 1:** `num = 4;`

```javascript
num = 4;
```

**Шаг 2:** `ans = num * num;`

```javascript
ans = 4 * 4 = 16;
```

**Шаг 3:** `return ans;` ⇒ возвращает 16

После этого управление возвращается к строке 7, и square4 становится равным 16

### ✅ Итоговые значения после выполнения кода:

| Идентификатор | Итоговое значение |
| ------------- | ----------------- |
| n             | 2                 |
| square        | код функции       |
| square2       | 4                 |
| square4       | 16                |

## 🎉 Резюме создания Контекста Выполнения:

| Шаг | Контекст                                | Событие                            |
| --- | --------------------------------------- | ---------------------------------- |
| 1   | Глобальный Контекст Выполнения          | Выделение памяти и выполнение кода |
| 2   | Контекст Выполнения функции (square(2)) | Создан для первого вызова          |
| 3   | Контекст Выполнения функции (square(4)) | Создан для второго вызова          |

## 📌 8. Что такое Стек Вызовов (Call Stack)?

👉 Это механизм, который отслеживает Контексты Выполнения.

Представьте Стек Вызовов как стопку книг 📚 — когда вы вызываете функцию,
вы кладёте новую книгу (контекст) наверх.
Когда функция завершается, книга снимается (pop) со стека.

### ✅ Пример работы Стека Вызовов на приведённом выше коде:

| Шаг | Стек Вызовов                                  |
| --- | --------------------------------------------- |
| 1   | Глобальный Контекст Выполнения                |
| 2   | Глобальный Контекст, затем контекст square(2) |
| 3   | Глобальный Контекст                           |
| 4   | Глобальный Контекст, затем контекст square(4) |
| 5   | Глобальный Контекст                           |

## 📌 9. Важные концепции, которые нужно запомнить:

- Глобальный Контекст Выполнения (GEC) создаётся при запуске JS
- Каждый вызов функции создаёт новый Контекст Выполнения
- Контексты Выполнения управляются с помощью Стека Вызовов
- Каждый контекст имеет две фазы: выделение памяти и выполнение кода
- Переменные изначально инициализируются как undefined в фазе памяти
- Функции поднимаются (hoisting) — весь код функции сохраняется в памяти
- Стек Вызовов обеспечивает правильный порядок выполнения кода
- После завершения функции её контекст удаляется из стека

## 📌 10. Итоговая визуализация (схематичное текстовое представление):

```
Start Program
|
|-- Глобальный Контекст Выполнения (добавлен в Стек Вызовов)
|     |
|     |-- Фаза памяти (n, square, square2, square4)
|     |-- Фаза выполнения кода (исполнение кода)
|          |
|          |-- square(2) (добавлен в Стек Вызовов)
|               |-- Функциональная фаза памяти (num, ans)
|               |-- Функциональная фаза кода (вычисление и возврат 4)
|          |-- square(2) (удалён из Стека Вызовов)
|          |
|          |-- square(4) (добавлен в Стек Вызовов)
|               |-- Функциональная фаза памяти (num, ans)
|               |-- Функциональная фаза кода (вычисление и возврат 16)
|          |-- square(4) (удалён из Стека Вызовов)
|
|-- Глобальный Контекст Выполнения (удалён из Стека Вызовов)
End Program
```

## 📦 ✨ Ключевые термины, которые нужно запомнить:

| Термин              | Значение                                                     |
| ------------------- | ------------------------------------------------------------ |
| Контекст Выполнения | Среда, в которой выполняется JS-код                          |
| Фаза памяти         | Выделяет место для переменных/функций                        |
| Фаза кода           | Выполнение кода построчно                                    |
| Стек Вызовов        | Стек, управляющий вызовами функций                           |
| Hoisting            | JS «поднимает» объявления функций и переменных в начало кода |
| Undefined           | Значение по умолчанию в фазе памяти                          |
| Вызов функции       | Вызов функции (например, square(2))                          |

```

```
