## Статические блоки инициализации в TypeScript

### 1. Что такое статический блок?

**Статический блок** — это специальный блок кода, который выполняется один раз при первой загрузке класса. Он служит для сложной инициализации статических свойств.

**Аналогия:** Если статические свойства — это "настройки по умолчанию", то статический блок — это "процедура настройки", которая может включать сложные вычисления или внешние зависимости.

### 2. Синтаксис статического блока

```typescript
class Counter {
  static count: number;

  // Статический блок инициализации
  static {
    console.log("Initializing counter class");
    this.count = 5; // или Counter.count = 5
  }
}
```

### 3. Ключевые особенности статических блоков

#### 1. Выполняется при первой загрузке класса

```typescript
class Counter {
  static count: number;

  static {
    console.log("Этот код выполнится ОДИН РАЗ при первом обращении к классу");
    Counter.count = 10;
  }
}

// Первое обращение к классу - блок выполняется
console.log(Counter.count); // Сначала выполнится статический блок, затем выведется 10

// Последующие обращения - блок НЕ выполняется
console.log(Counter.count); // Просто выведется 10
```

#### 2. Доступ к статическим членам через `this` или имя класса

```typescript
class Counter {
  static count: number;

  static {
    // Оба варианта работают:
    this.count = 5; // через this
    Counter.count = 10; // через имя класса (предпочтительнее)
  }
}
```

### 4. Практический пример автора

#### Исходный код:

```typescript
// Внешняя функция (может быть в другом файле/модуле)
function loadInitialCount(): number {
  return 10; // Например, загружаем из конфига или API
}

class Counter {
  static count: number;

  // Статический блок
  static {
    console.log("Initializing counter class");
    Counter.count = loadInitialCount(); // Зависимость от внешней функции
  }

  static increment() {
    Counter.count += 1;
  }
}

// Использование:
Counter.increment();
console.log(Counter.count); // 11
```

### 5. Когда выполняется статический блок?

**Момент выполнения:** При первом обращении к классу любым способом:

```typescript
class Logger {
  static initialized: boolean;

  static {
    console.log("Logger class initialized");
    this.initialized = true;
  }
}

// Любое из этих действий вызовет статический блок:
const log = new Logger(); // ✅ Создание экземпляра
Logger.initialized; // ✅ Обращение к статическому свойству
Logger.someMethod(); // ✅ Вызов статического метода
```

### 6. Сравнение с конструктором

| Аспект                 | Конструктор                | Статический блок                    |
| ---------------------- | -------------------------- | ----------------------------------- |
| **Когда выполняется**  | При каждом `new Class()`   | Один раз при первой загрузке класса |
| **Область видимости**  | Уровень экземпляра         | Уровень класса                      |
| **Доступ к данным**    | Только нестатические члены | Только статические члены            |
| **Количество вызовов** | Много раз                  | Один раз                            |

#### Визуализация времени выполнения:

```typescript
class Example {
  static staticValue: number;
  instanceValue: number;

  // Выполняется 1 раз при загрузке класса
  static {
    console.log("Статический блок - выполняется 1 раз");
    this.staticValue = 100;
  }

  // Выполняется при КАЖДОМ создании объекта
  constructor() {
    console.log("Конструктор - выполняется много раз");
    this.instanceValue = 200;
  }
}

// В консоли:
const obj1 = new Example(); // "Статический блок", "Конструктор"
const obj2 = new Example(); // "Конструктор" (статический блок не выполняется)
```

### 7. Практические сценарии использования

#### Сценарий 1: Загрузка конфигурации

```typescript
class AppConfig {
  static apiUrl: string;
  static timeout: number;

  static {
    // Загружаем конфиг из внешнего источника
    const config = loadConfigFromFile(); // Предположим, есть такая функция
    this.apiUrl = config.apiUrl;
    this.timeout = config.timeout;

    console.log("AppConfig initialized with:", this.apiUrl, this.timeout);
  }
}

// Где-то в коде:
fetch(AppConfig.apiUrl, { timeout: AppConfig.timeout });
```

#### Сценарий 2: Сложная инициализация

```typescript
class Database {
  static connectionPool: ConnectionPool;
  static isConnected: boolean;

  static {
    try {
      this.connectionPool = new ConnectionPool();
      this.connectionPool.initialize();
      this.isConnected = true;
      console.log("Database connection pool initialized");
    } catch (error) {
      this.isConnected = false;
      console.error("Failed to initialize database", error);
    }
  }
}
```

#### Сценарий 3: Регистрация в системе

```typescript
class Plugin {
  static plugins: Map<string, Plugin> = new Map();
  static defaultPlugin: Plugin;

  static {
    // Автоматическая регистрация при загрузке класса
    const defaultPlugin = new Plugin("default");
    this.plugins.set("default", defaultPlugin);
    this.defaultPlugin = defaultPlugin;
  }

  constructor(public name: string) {}
}
```

### 8. Работа с внешними зависимостями

Статические блоки идеально подходят для зависимостей, которые не являются частью класса:

```typescript
// Внешние функции (могут быть в других модулях)
function fetchConfigFromAPI(): Promise<Config> {
  /* ... */
}
function readConfigFile(): Config {
  /* ... */
}

class Settings {
  static apiKey: string;
  static environment: string;

  static {
    // Выбираем способ загрузки конфига в зависимости от окружения
    if (process.env.NODE_ENV === "production") {
      const config = readConfigFile();
      this.apiKey = config.apiKey;
      this.environment = config.environment;
    } else {
      // development - другие настройки
      this.apiKey = "dev-key";
      this.environment = "development";
    }
  }
}
```

### 9. Обработка ошибок в статических блоках

```typescript
class CriticalService {
  static isReady: boolean;

  static {
    try {
      // Пытаемся инициализировать критически важный сервис
      initializeCriticalService();
      this.isReady = true;
    } catch (error) {
      this.isReady = false;
      console.error("Critical service failed to initialize:", error);
      // Можно даже прервать выполнение программы
      // process.exit(1);
    }
  }
}
```

### 10. Множественные статические блоки

В классе может быть несколько статических блоков - они выполняются в порядке объявления:

```typescript
class MultiBlock {
  static step1: string;
  static step2: string;
  static step3: string;

  static {
    console.log("Блок 1");
    this.step1 = "первый";
  }

  static {
    console.log("Блок 2");
    this.step2 = "второй";
  }

  static {
    console.log("Блок 3");
    this.step3 = "третий";
  }
}
// Выведет: "Блок 1", "Блок 2", "Блок 3"
```

### 11. Лучшие практики

1. **Используйте для сложной инициализации** - когда простого присваивания недостаточно
2. **Обрабатывайте ошибки** - статические блоки могут падать
3. **Избегайте боковых эффектов** - кроме инициализации статических свойств
4. **Документируйте зависимости** - если блок зависит от внешних ресурсов

### Ключевые выводы:

1. **Статический блок = конструктор для класса** - выполняется один раз при загрузке
2. **Для сложной инициализации** - когда нужна логика, а не просто присваивание
3. **Зависимости от внешних ресурсов** - конфиги, файлы, API
4. **Один раз за время жизни приложения** - в отличие от конструктора
5. **Только для статических членов** - нельзя обращаться к нестатическим свойствам

Статические блоки — это мощный инструмент для обеспечения правильной инициализации классов перед их использованием в приложении! 🚀
