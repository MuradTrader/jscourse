## Private модификатор доступа - максимальная инкапсуляция

### 1. Теоретическое напоминание

**Private модификатор** — самый строгий уровень доступа:

- ✅ Доступен только внутри класса, где объявлен
- ❌ Недоступен в дочерних классах
- ❌ Недоступен в экземплярах класса (объектах)

### 2. Преобразование protected в private

Автор меняет свойство `phone` с `protected` на `private`:

```typescript
class User {
  public name: string;
  public email: string;
  private phone: number; // Было protected, стало private

  constructor(name: string, email: string, phone: number) {
    this.name = name;
    this.email = email;
    this.phone = phone;
  }
}
```

### 3. Что изменилось при смене на private

#### Было (protected):

```typescript
class Admin extends User {
  public printPhone() {
    console.log(this.phone); // ✅ Работало - protected доступно в наследниках
  }
}
```

#### Стало (private):

```typescript
class Admin extends User {
  public printPhone() {
    console.log(this.phone); // ❌ ОШИБКА: Property 'phone' is private
  }
}
```

### 4. Доступ к private свойству в том же классе

**Ключевой момент:** Private свойства доступны **только** в том классе, где объявлены:

```typescript
class User {
  private phone: number;

  public printPhone() {
    // ✅ Доступ внутри того же класса
    console.log(this.phone);
  }

  private validatePhone() {
    // ✅ Доступ внутри того же класса
    return this.phone > 0;
  }
}
```

### 5. Практическая демонстрация автора

Автор показывает, как работать с private свойствами:

```typescript
class User {
  private phone: number;

  constructor(phone: number) {
    this.phone = phone;
  }

  // Public метод для доступа к private свойству
  public printPhone() {
    console.log(this.phone); // ✅ Работает - внутри класса User
  }
}

class Admin extends User {
  // НЕТ доступа к this.phone здесь!
}

// Использование:
const user = new User(123456789);
user.printPhone(); // ✅ 123456789 - через public метод
// user.phone;     // ❌ ОШИБКА - прямое обращение к private свойству
```

### 6. Сравнение всех модификаторов доступа

| Контекст                     | Public | Protected | Private |
| ---------------------------- | ------ | --------- | ------- |
| **Внутри класса объявления** | ✅     | ✅        | ✅      |
| **В дочерних классах**       | ✅     | ✅        | ❌      |
| **В экземплярах объектов**   | ✅     | ❌        | ❌      |

### 7. Почему private важен?

#### Инкапсуляция внутренней логики:

```typescript
class BankAccount {
  private balance: number;
  private accountId: string;

  public deposit(amount: number) {
    if (this.validateAmount(amount)) {
      this.balance += amount;
    }
  }

  private validateAmount(amount: number) {
    return amount > 0;
  }

  public getBalance() {
    return this.balance; // Контролируемый доступ
  }
}
```

#### Защита чувствительных данных:

```typescript
class User {
  private password: string;
  private authToken: string;

  public login(enteredPassword: string) {
    return this.validatePassword(enteredPassword);
  }

  private validatePassword(input: string) {
    // Сложная логика проверки пароля
    return this.encrypt(input) === this.password;
  }

  private encrypt(data: string) {
    // Шифрование данных
  }
}
```

### 8. Стратегия работы с private свойствами

Поскольку private свойства недоступны извне, нужно создавать public методы для взаимодействия с ними:

```typescript
class User {
  private phone: number;

  // Getter для чтения
  public getPhone(): number {
    return this.phone;
  }

  // Setter для записи с валидацией
  public setPhone(newPhone: number) {
    if (this.isValidPhone(newPhone)) {
      this.phone = newPhone;
    }
  }

  private isValidPhone(phone: number): boolean {
    return phone.toString().length >= 10;
  }
}
```

### 9. Что происходит в примере автора

```typescript
class User {
  private phone: number;

  public printPhone() {
    console.log(this.phone); // ✅ Доступ в том же классе
  }
}

class Admin extends User {
  // Любая попытка доступа к this.phone будет ошибкой
}

const user = new User(123456789);
user.printPhone(); // ✅ Работает - выводит 123456789
// user.phone;     // ❌ Ошибка - прямое обращение к private
```

### 10. Когда использовать private?

**Используйте private для:**

- Чувствительных данных (пароли, токены, персональная информация)
- Внутренней логики, которая не должна меняться наследниками
- Вспомогательных методов, которые используются только внутри класса
- Свойств, которые требуют строгого контроля доступа

**Примеры:**

```typescript
class DatabaseConnection {
  private connectionString: string;
  private isConnected: boolean;

  private connectToDatabase() {}
  private validateConnection() {}

  public executeQuery(query: string) {
    if (!this.isConnected) {
      this.connectToDatabase();
    }
    // выполнение запроса
  }
}
```

### 11. Ограничения наследования с private

Private свойства создают жесткие границы в иерархии наследования:

```typescript
class User {
  private secretKey: string;
}

class SuperAdmin extends User {
  // Не может получить доступ к secretKey
  // Должен использовать public методы User для работы с ним
}
```

### Ключевые выводы:

1. **Private = полная изоляция** - доступ только в классе объявления
2. **Не наследуется** - дочерние классы не видят private свойства/методы
3. **Public интерфейс** - нужны public методы для взаимодействия с private данными
4. **Максимальная инкапсуляция** - идеально для чувствительных данных
5. **Жесткие границы** - создает четкое разделение ответственности

Private модификатор — это мощный инструмент для создания безопасных, надежных классов с четко определенными границами ответственности! 🔒

Теперь у вас есть полное понимание всех трех модификаторов доступа в TypeScript! 🎓
