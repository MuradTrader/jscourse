## Public модификатор доступа - практическое применение

### 1. Теоретическое напоминание

**Public модификатор** — самый доступный уровень:

- ✅ Доступен внутри класса
- ✅ Доступен в дочерних классах
- ✅ Доступен в экземплярах класса (объектах)

### 2. Public по умолчанию

**Важнейшее правило:** Если модификатор не указан, TypeScript автоматически считает свойство/метод **public**.

```typescript
class User {
  name: string; // Автоматически public
  email: string; // Автоматически public
  greet() {} // Автоматически public
}
```

**Эквивалентно:**

```typescript
class User {
  public name: string;
  public email: string;
  public greet() {}
}
```

### 3. Явное указание public

Хотя это избыточно, можно явно указать `public`:

```typescript
class User {
  public name: string; // Явно public
  email: string; // Неявно public
  public greet() {} // Явно public
}
```

### 4. Проверка доступа в трех контекстах

#### Контекст 1: Внутри самого класса

```typescript
class User {
  public name: string = "John";

  public greet() {
    // ✅ Доступ к public свойству внутри класса
    return `Hello ${this.name}`;
  }
}
```

**Объяснение:** В методе `greet()` мы используем `this.name` - это работает, потому что:

- `this` ссылается на текущий экземпляр класса
- `name` объявлен как `public`
- Мы находимся внутри того же класса

#### Контекст 2: В дочернем классе

```typescript
class User {
  public name: string = "John";
}

class Admin extends User {
  public printName() {
    // ✅ Доступ к public свойству родителя из дочернего класса
    console.log(this.name);
  }
}
```

**Объяснение:** В классе `Admin` мы наследуем свойство `name` от `User` и можем к нему обращаться через `this.name`, потому что:

- `Admin` наследует все public свойства `User`
- `this` в `Admin` ссылается на экземпляр `Admin`, который содержит унаследованные свойства

#### Контекст 3: В экземпляре класса (объекте)

```typescript
class User {
  public name: string = "John";
}

const user = new User();
// ✅ Доступ к public свойству извне класса
console.log(user.name); // "John"
```

**Объяснение:** Мы создали объект `user` и можем напрямую обращаться к его public свойствам.

### 5. Практическая демонстрация автора

Автор показывает полный пример:

```typescript
class User {
  public name: string;
  public email: string;
  lastName?: string; // Неявно public

  constructor(name: string, email: string, lastName?: string) {
    this.name = name;
    this.email = email;
    this.lastName = lastName;
  }

  public greet() {
    return `Hello ${this.name}`; // ✅ Доступ внутри класса
  }
}

class Admin extends User {
  public printName() {
    // ✅ Доступ в дочернем классе
    console.log(this.name);
  }
}

// Использование:
const user = new User("John", "john@email.com");
const admin = new Admin("Mark", "mark@email.com");

// ✅ Доступ через экземпляр родителя
console.log(user.name); // "John"

// ✅ Доступ через экземпляр потомка
console.log(admin.name); // "Mark"

// ✅ Вызов public метода потомка
admin.printName(); // Выведет "Mark"
```

### 6. Ключевые моменты о ключевом слове `this`

Автор подчеркивает важность `this`:

- **`this` всегда ссылается на текущий экземпляр класса**
- В дочернем классе `this` содержит как унаследованные, так и собственные свойства
- Через `this` можно обращаться к public свойствам родительского класса

### 7. Итоговый вывод

**Public свойства и методы:**

- Явно или неявно доступны везде
- Могут использоваться внутри класса, в наследниках и извне
- Являются "интерфейсом" класса для внешнего мира
- По умолчанию все члены класса - public

### 8. Когда использовать public?

**Используйте public для:**

- Данных, которые должны быть доступны извне
- Методов, которые являются публичным API класса
- Свойств, которые могут безопасно изменяться извне

**Пример:**

```typescript
class BankAccount {
  public accountNumber: string; // Номер счета - публичная информация
  public getBalance() {} // Получение баланса - публичная операция
  // private balance: number;   // Сам баланс - приватный!
}
```

### 9. Что дальше?

Автор намекает, что в следующих видео будут рассмотрены:

- **protected** - более строгий доступ
- **private** - самый строгий доступ
- Различия между ними и практические сценарии использования

### Ключевые выводы:

1. **Public = полная доступность** - везде, всегда
2. **По умолчанию все public** - если модификатор не указан
3. **`this` работает ожидаемо** - в наследниках получает родительские свойства
4. **Public - это интерфейс** - что видят другие части программы
5. **TypeScript проверяет доступ** - но для public ограничений нет

Public модификатор - это основа взаимодействия между классами и внешним миром! 🚀
