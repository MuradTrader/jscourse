## Сеттеры (мутаторы) в TypeScript - контролируемое изменение свойств

### 1. Проблема, которую решают сеттеры

Автор напоминает две основные проблемы:

1. **Валидация возраста** - нужно проверять корректность перед установкой
2. **Вычисляемые свойства** - `fullName` должен быть свойством, а не методом

### 2. Что такое сеттер (мутатор)?

**Сеттер** — это специальный метод, который выглядит и используется как свойство, но содержит логику, выполняемую при попытке установить значение.

**Ключевая особенность:** Вы вызываете сеттер как обычное свойство, но на самом деле выполняется функция с вашей логикой.

### 3. Синтаксис объявления сеттера

```typescript
class Person {
  private _age?: number; // Приватное свойство для хранения возраста

  // Сеттер для возраста
  public set age(age: number) {
    // Логика валидации
    if (age > 200 || age < 0) {
      throw new Error("The age must be within the age range 0 to 200");
    }
    this._age = age; // Устанавливаем значение в приватное свойство
  }
}
```

### 4. Детальный разбор кода автора

#### Шаг 1: Удаление age из конструктора

**Было:**

```typescript
constructor(
    public firstName: string,
    public lastName: string,
    public age: number  // УДАЛЯЕМ отсюда
) {}
```

**Почему удаляем?** Потому что теперь возраст будет устанавливаться через сеттер, а не через конструктор.

#### Шаг 2: Создание приватного свойства

```typescript
private _age?: number;
```

**Объяснение:**

- `private` - свойство доступно только внутри класса
- `_age` - соглашение об именовании (underscore prefix) для приватных свойств
- `?` - свойство может быть undefined, так как не инициализировано в конструкторе
- `number` - тип данных

#### Шаг 3: Создание сеттера

```typescript
public set age(age: number) {
    if (age > 200 || age < 0) {
        throw new Error("The age must be within the age range 0 to 200");
    }
    this._age = age;
}
```

**Разбор синтаксиса:**

- `public` - сеттер доступен извне класса
- `set` - ключевое слово для объявления сеттера
- `age` - имя свойства, которое будут использовать извне
- `(age: number)` - параметр (значение, которое пытаются установить)

### 5. Как использовать сеттер?

**Вот магия сеттеров:** Вы используете их как обычные свойства!

```typescript
const john = new Person("John", "Doe");
john.age = 45; // Вызывается сеттер age(45)
```

**Сравнение с обычным методом:**

```typescript
// Обычный метод (неудобно):
john.setAge(45);

// Сеттер (естественно):
john.age = 45;
```

### 6. Что происходит при установке значения?

Когда вы пишете `john.age = 45`:

1. **Автоматически вызывается** сеттер `age(45)`
2. **Выполняется валидация** - проверка на диапазон 0-200
3. **Если валидация прошла** - значение сохраняется в `this._age`
4. **Если валидация не прошла** - выбрасывается ошибка

### 7. Примеры использования с валидацией

#### ✅ Корректные значения:

```typescript
const john = new Person("John", "Doe");
john.age = 25; // Работает
john.age = 0; // Работает
john.age = 200; // Работает
```

#### ❌ Некорректные значения:

```typescript
const john = new Person("John", "Doe");
john.age = 201; // ❌ ОШИБКА: "The age must be within the age range 0 to 200"
john.age = -5; // ❌ ОШИБКА: "The age must be within the age range 0 to 200"
```

### 8. Проблема: нельзя прочитать значение

**Текущая проблема:**

```typescript
const john = new Person("John", "Doe");
john.age = 45;
console.log(john.age); // undefined ❌
```

**Почему undefined?**

- У нас есть только **сеттер** для записи
- Нет **геттера** для чтения
- Свойство `age` теперь ведет себя как метод записи, но не как свойство чтения

### 9. Преимущества подхода с сеттером

#### 1. Инкапсуляция логики валидации

```typescript
// Вся логика валидации в одном месте
public set age(age: number) {
    if (age > 200 || age < 0) throw new Error("Invalid age");
    if (!Number.isInteger(age)) throw new Error("Age must be integer");
    this._age = age;
}
```

#### 2. Естественный интерфейс

```typescript
// Интуитивно понятно
person.age = 25; // Вместо person.setAge(25)
```

#### 3. Защита от некорректных данных

```typescript
// Невозможно установить некорректный возраст
// Ошибка будет выброшена сразу при попытке установки
```

#### 4. Гибкость для изменений

```typescript
// Можно легко изменить логику валидации без изменения внешнего интерфейса
public set age(age: number) {
    // Меняем правила - внешний код не знает об этом
    if (age > 150) throw new Error("Too old");
    this._age = age;
}
```

### 10. Текущее состояние класса

```typescript
class Person {
  private _age?: number;

  constructor(public firstName: string, public lastName: string) {}

  public set age(age: number) {
    if (age > 200 || age < 0) {
      throw new Error("The age must be within the age range 0 to 200");
    }
    this._age = age;
  }

  // Пока нет геттера - поэтому чтение age возвращает undefined
  public fullName(): string {
    return `${this.firstName} ${this.lastName}`;
  }
}
```

### 11. Что будет в следующем видео?

Автор обещает решить проблему чтения через **геттеры (аксессоры)**:

```typescript
// Будет добавлен геттер:
public get age(): number {
    if (this._age === undefined) {
        throw new Error("Age not set");
    }
    return this._age;
}
```

### Ключевые выводы:

1. **Сеттер = умная установка значения** - выглядит как свойство, работает как метод
2. **Синтаксис `set propertyName(value)`** - ключевое слово `set`
3. **Использование как обычного свойства** - `obj.property = value`
4. **Идеально для валидации** - проверка перед установкой
5. **Нужен геттер для чтения** - без него свойство нельзя прочитать

Сеттеры решают проблему контролируемого изменения свойств, обеспечивая валидацию и инкапсуляцию! 🛡️
