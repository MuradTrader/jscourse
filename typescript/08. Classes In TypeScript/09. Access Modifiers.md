## Модификаторы доступа в TypeScript

### 1. Что такое модификаторы доступа?

**Модификаторы доступа** — это ключевые слова TypeScript, которые контролируют видимость свойств и методов класса. Они определяют, откуда можно получить доступ к определенным членам класса.

**Ключевой момент:** Это функция **только TypeScript**. В JavaScript модификаторов доступа не существует — они добавляются TypeScript для повышения безопасности типов и инкапсуляции.

### 2. Зачем нужны модификаторы доступа?

Они помогают реализовать принцип **инкапсуляции** — сокрытие внутренней реализации класса и предоставление контролируемого доступа к данным.

**Аналогия:** Представьте банковский счет:

- **public** — ваш номер счета (все могут видеть)
- **protected** — информация для банковских сотрудников и систем
- **private** — PIN-код карты (только вы знаете)

### 3. Три типа модификаторов доступа

#### Public (публичный) - самый доступный

```typescript
public propertyName: type;
```

**Доступен:**

- ✅ Внутри самого класса
- ✅ В дочерних классах
- ✅ В экземплярах класса (объектах)

#### Protected (защищенный) - средний уровень

```typescript
protected propertyName: type;
```

**Доступен:**

- ✅ Внутри самого класса
- ✅ В дочерних классах
- ❌ В экземплярах класса (объектах)

#### Private (приватный) - самый строгий

```typescript
private propertyName: type;
```

**Доступен:**

- ✅ Внутри самого класса
- ❌ В дочерних классах
- ❌ В экземплярах класса (объектах)

### 4. Как применять модификаторы доступа

Модификаторы указываются **перед** объявлением свойства или метода:

```typescript
class User {
  // Свойства с модификаторами
  public name: string;
  private email: string;
  protected phoneNumber: string;

  // Методы с модификаторами
  public greet() {
    return `Hello ${this.name}`;
  }

  private validateEmail() {
    // Логика проверки email
  }

  protected updatePhone() {
    // Логика обновления телефона
  }
}
```

### 5. Детальная таблица видимости

| Модификатор   | Текущий класс | Дочерние классы | Экземпляры объектов |
| ------------- | ------------- | --------------- | ------------------- |
| **public**    | ✅            | ✅              | ✅                  |
| **protected** | ✅            | ✅              | ❌                  |
| **private**   | ✅            | ❌              | ❌                  |

### 6. Практические примеры доступа

#### Public доступ

```typescript
class User {
  public name: string = "John";
}

const user = new User();
console.log(user.name); // ✅ Работает - public свойство
```

#### Protected доступ

```typescript
class User {
  protected phone: string = "123-456-789";
}

class Admin extends User {
  showPhone() {
    console.log(this.phone); // ✅ Работает в дочернем классе
  }
}

const user = new User();
console.log(user.phone); // ❌ Ошибка - protected свойство
```

#### Private доступ

```typescript
class User {
  private password: string = "secret";

  checkPassword(input: string) {
    return this.password === input; // ✅ Работает в своем классе
  }
}

class Admin extends User {
  getPassword() {
    return this.password; // ❌ Ошибка - private свойство
  }
}

const user = new User();
console.log(user.password); // ❌ Ошибка - private свойство
```

### 7. Важные особенности

#### Значение по умолчанию

Если модификатор не указан, свойство считается **public**:

```typescript
class User {
  name: string; // Автоматически public
}
```

#### Применение к методам

Модификаторы работают и для методов:

```typescript
class User {
  public getPublicInfo() {} // Доступен везде
  protected getInternalInfo() {} // Только в классах
  private getSecretInfo() {} // Только в этом классе
}
```

#### TypeScript проверяет доступ

TypeScript будет выдавать ошибки компиляции при нарушении правил доступа — это предотвращает ошибки на этапе разработки.

### 8. Зачем это нужно?

1. **Безопасность** — скрываем чувствительные данные (пароли, токены)
2. **Инкапсуляция** — скрываем внутреннюю реализацию
3. **Контроль изменений** — предотвращаем случайное изменение важных данных
4. **Чистый интерфейс** — показываем только то, что нужно использовать извне

### 9. Что будет в следующих лекциях

Автор обещает практическое погружение с реальными примерами:

- Конкретные сценарии использования каждого модификатора
- Как правильно проектировать классы с модификаторами доступа
- Лучшие практики и типичные ошибки

### Ключевые выводы:

1. **public** — полная доступность (по умолчанию)
2. **protected** — доступ только в иерархии классов
3. **private** — доступ только в объявляющем классе
4. **TypeScript-only фича** — компилируется в обычный JavaScript
5. **Инкапсуляция** — основная цель использования

Модификаторы доступа — это мощный инструмент для создания безопасных и поддерживаемых приложений! Они помогают писать код, который сложнее сломать случайно. 🛡️
