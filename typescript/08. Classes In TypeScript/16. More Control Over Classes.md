## Проблемы валидации и вычисляемых свойств в классах

### 1. Создание класса Person

Автор начинает с создания простого класса для демонстрации проблем:

```typescript
class Person {
  constructor(
    public firstName: string,
    public lastName: string,
    public age: number
  ) {}
}
```

**Проблема:** Этот класс слишком "глупый" - он принимает любые значения без проверки.

### 2. Проблема №1: Отсутствие валидации данных

#### Некорректные данные:

```typescript
const john = new Person("John", "Doe", 210); // 210 лет - нереально
const mark = new Person("Mark", "Smith", -10); // Отрицательный возраст - бессмыслица
```

#### Наивное решение - валидация в конструкторе:

```typescript
class Person {
  constructor(
    public firstName: string,
    public lastName: string,
    public age: number
  ) {
    // Проблемный подход - валидация в конструкторе
    if (age > 200 || age < 0) {
      throw new Error("The age must be within the age range 0 to 200");
    }
  }
}
```

**Проблемы этого подхода:**

1. **Нарушение Single Responsibility Principle** - конструктор делает слишком много
2. **Сложность тестирования** - трудно тестировать разные сценарии валидации
3. **Повторное использование** - валидацию нельзя переиспользовать в других местах
4. **Сложность поддержки** - при добавлении новых правил конструктор становится слишком сложным

### 3. Проблема №2: Вычисляемые свойства

#### Неудобный подход - метод:

```typescript
class Person {
  // ... свойства

  public fullName(): string {
    return `${this.firstName} ${this.lastName}`;
  }
}

// Использование:
const john = new Person("John", "Doe", 30);
console.log(john.fullName()); // Нужно вызывать как метод
```

**Проблема:** Пользователи класса ожидают, что `fullName` - это свойство, а не метод.

#### Альтернативное решение - свойство в конструкторе:

```typescript
class Person {
  public fullName: string;

  constructor(
    public firstName: string,
    public lastName: string,
    public age: number
  ) {
    this.fullName = `${firstName} ${lastName}`;

    // И еще валидация...
    if (age > 200 || age < 0) {
      throw new Error("Invalid age");
    }
  }
}
```

**Проблемы этого подхода:**

1. **Конструктор становится перегруженным** - слишком много ответственности
2. **Дублирование логики** - если имя или фамилия меняются, fullName не обновляется автоматически
3. **Нарушение инкапсуляции** - fullName можно изменить извне, что приведет к inconsistency

### 4. Демонстрация проблем в коде

#### Пример с "ломающимся" fullName:

```typescript
class Person {
  public fullName: string;

  constructor(
    public firstName: string,
    public lastName: string,
    public age: number
  ) {
    this.fullName = `${firstName} ${lastName}`;
  }
}

const person = new Person("John", "Doe", 30);
console.log(person.fullName); // "John Doe"

// Проблема: меняем firstName, но fullName не обновляется!
person.firstName = "Mike";
console.log(person.fullName); // Все еще "John Doe" - НЕПРАВИЛЬНО!
```

## Что на самом деле происходит в коде:

```typescript
class Person {
  public fullName: string;

  constructor(
    public firstName: string, // public - можно менять
    public lastName: string, // public - можно менять
    public age: number // public - можно менять
  ) {
    this.fullName = `${firstName} ${lastName}`;
  }
}

const person = new Person("John", "Doe", 30);
console.log(person.fullName); // "John Doe"

// ЭТО РАБОТАЕТ ПРАВИЛЬНО:
person.firstName = "Mike";
console.log(person.firstName); // "Mike" - изменилось!
console.log(person.fullName); // "John Doe" - а это НЕ изменилось!
```

## Что меняется, а что нет:

✅ **Меняется:** `person.firstName` - теперь равно "Mike"  
✅ **Меняется:** `person.lastName` - если бы мы меняли его  
✅ **Меняется:** `person.age` - если бы мы меняли его

❌ **Не меняется:** `person.fullName` - остается "John Doe"

## Почему fullName не меняется?

Проблема не в том, что `firstName` не меняется, а в том, что **`fullName` не связан динамически с `firstName` и `lastName`**.

**Что происходит по шагам:**

1. **Создание объекта:**

   ```typescript
   const person = new Person("John", "Doe", 30);
   // В конструкторе выполняется:
   // this.fullName = "John" + " " + "Doe"; → "John Doe"
   ```

2. **Изменение firstName:**

   ```typescript
   person.firstName = "Mike";
   // firstName теперь "Mike", но fullName уже был вычислен и сохранен
   ```

3. **Проверка fullName:**
   ```typescript
   console.log(person.fullName); // "John Doe"
   // fullName все еще содержит старое значение, потому что
   // он был вычислен ОДИН РАЗ в конструкторе и больше не пересчитывается
   ```

## Визуализация состояния объекта:

**До изменения:**

```typescript
person = {
  firstName: "John",
  lastName: "Doe",
  age: 30,
  fullName: "John Doe", // вычислено в конструкторе
};
```

**После изменения:**

```typescript
person = {
  firstName: "Mike", // ИЗМЕНИЛОСЬ!
  lastName: "Doe",
  age: 30,
  fullName: "John Doe", // НЕ ИЗМЕНИЛОСЬ!
};
```

## Доказательство, что firstName меняется:

```typescript
const person = new Person("John", "Doe", 30);
console.log("До:", person.firstName); // "John"

person.firstName = "Mike";
console.log("После:", person.firstName); // "Mike" - УБЕДИТЕСЬ САМИ!

// А вот это покажет проблему:
console.log("fullName до:", person.fullName); // "John Doe"
console.log("fullName после:", person.fullName); // "John Doe" - проблема!
```

## В чем настоящая проблема?

**Проблема в логике класса**, а не в том, что свойства не меняются:

- `fullName` вычисляется **один раз** при создании объекта
- При изменении `firstName` или `lastName` значение `fullName` **не обновляется автоматически**
- Это приводит к **несогласованности данных** в объекте

#### Пример с раздутым конструктором:

```typescript
class Person {
  public fullName: string;
  public isAdult: boolean;
  public ageGroup: string;

  constructor(
    public firstName: string,
    public lastName: string,
    public age: number
  ) {
    // Валидация
    if (age > 200 || age < 0) {
      throw new Error("Invalid age");
    }

    // Вычисляемые свойства
    this.fullName = `${firstName} ${lastName}`;
    this.isAdult = age >= 18;

    // Еще больше логики...
    if (age < 13) this.ageGroup = "Child";
    else if (age < 20) this.ageGroup = "Teenager";
    else if (age < 65) this.ageGroup = "Adult";
    else this.ageGroup = "Senior";

    // И так может продолжаться...
  }
}
```

### 5. Анализ архитектурных проблем

#### Нарушение принципов SOLID:

1. **Single Responsibility** - конструктор делает валидацию, вычисления, инициализацию
2. **Open/Closed** - трудно расширять функциональность без изменения конструктора
3. **DRY (Don't Repeat Yourself)** - валидационная логика заперта в конструкторе

#### Проблемы поддерживаемости:

- **Хрупкость** - изменения в одном месте ломают другие functionality
- **Тестируемость** - трудно тестировать изолированно валидацию и вычисления
- **Расширяемость** - добавление новых правил требует модификации конструктора

### 6. Идеальное решение должно обеспечивать:

1. **Автоматическую валидацию** при установке значений
2. **Автоматическое вычисление** производных свойств
3. **Чистый интерфейс** - свойства должны вести себя как свойства, не как методы
4. **Инкапсуляцию** - внутренняя логика скрыта от пользователей класса
5. **Гибкость** - легко добавлять новые правила и вычисления

### 7. Что обещает автор в следующем видео?

Автор намекает на решение через:

- **Мутаторы (сеттеры)** - для контролируемого изменения свойств с валидацией
- **Аксессоры (геттеры)** - для вычисляемых свойств, которые ведут себя как обычные свойства

### 8. Конкретные сценарии для решения

#### Для возраста нужен:

- **Валидация** при установке значения
- **Автоматический отказ** при некорректных данных

#### Для fullName нужен:

- **Автоматическое вычисление** на основе firstName и lastName
- **"Волшебное" свойство** - доступ как к свойству, но с логикой как у метода

### Ключевые выводы:

1. **Прямое присваивание свойств опасно** - нет контроля над данными
2. **Конструктор не должен делать всё** - это антипаттерн
3. **Методы vs Свойства** - пользователи ожидают интуитивного интерфейса
4. **Нужен механизм** для умных свойств с логикой
5. **TypeScript предоставляет решение** - аксессоры и мутаторы

Именно эти проблемы решают геттеры и сеттеры в TypeScript, которые автор покажет в следующем видео! 🎯

Готовы переходить к изучению аксессоров и мутаторов?
