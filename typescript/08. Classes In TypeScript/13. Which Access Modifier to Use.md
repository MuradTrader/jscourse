## Стратегия выбора модификаторов доступа

### 1. Основная философия

Автор предлагает **консервативный подход** - начинать с самого строгого ограничения и ослаблять его только при необходимости:

```
Private → Protected → Public
```

**Принцип минимальных привилегий:** Давать только тот доступ, который действительно необходим.

### 2. Пошаговая стратегия выбора

#### Шаг 1: Начинайте с Private

```typescript
class User {
  private internalData: string; // По умолчанию - самый строгий доступ

  private calculateInternalValue() {
    // Внутренняя логика, не нужная снаружи
  }
}
```

**Мыслительный процесс:** "Нужен ли этот метод/свойство кому-либо кроме этого класса?"

- **Если НЕТ** → оставляйте `private`

#### Шаг 2: При необходимости → Protected

```typescript
class User {
  protected dataForChildren: string; // Нужно наследникам

  protected methodForChildren() {
    // Логика для наследников
  }
}
```

**Мыслительный процесс:** "Нужен ли доступ наследникам этого класса?"

- **Если ДА** → меняйте на `protected`
- **Если НЕТ** → оставляйте `private`

#### Шаг 3: В крайнем случае → Public

```typescript
class User {
  public apiMethod: string; // Должен быть доступен извне

  public publicInterface() {
    // Публичное API класса
  }
}
```

**Мыслительный процесс:** "Должен ли этот метод/свойство быть частью публичного интерфейса?"

- **Если ДА** → меняйте на `public`
- **Если НЕТ** → оставляйте `protected` или `private`

### 3. Практические примеры применения стратегии

#### Пример 1: Банковский счет

```typescript
class BankAccount {
  // Шаг 1: Начинаем с private
  private balance: number;
  private transactionHistory: string[];

  // Шаг 2: Для наследников - protected
  protected accountType: string;

  // Шаг 3: Публичное API - public
  public getBalance(): number {
    return this.balance;
  }

  public deposit(amount: number): void {
    // Использует private методы внутри
    if (this.validateAmount(amount)) {
      this.balance += amount;
      this.recordTransaction(`Deposit: ${amount}`);
    }
  }

  private validateAmount(amount: number): boolean {
    return amount > 0;
  }

  private recordTransaction(description: string): void {
    this.transactionHistory.push(description);
  }
}
```

#### Пример 2: Система аутентификации

```typescript
class Authenticator {
  // Private - внутренние данные
  private encryptionKey: string;
  private sessionTokens: Map<string, Date>;

  // Protected - для специализированных аутентификаторов
  protected maxLoginAttempts: number = 5;

  // Public - интерфейс для использования
  public login(username: string, password: string): boolean {
    const isValid = this.validateCredentials(username, password);
    if (isValid) {
      this.createSession(username);
    }
    return isValid;
  }

  private validateCredentials(username: string, password: string): boolean {
    // Сложная логика проверки
    return true;
  }

  private createSession(username: string): void {
    // Логика создания сессии
  }
}
```

### 4. Почему такая стратегия эффективна?

#### Безопасность

```typescript
// ❌ Опасный подход - все public по умолчанию
class User {
  public password: string; // Кто угодно может изменить пароль!
  public isAdmin: boolean; // Кто угодно может стать админом!
}

// ✅ Безопасный подход - консервативный
class User {
  private password: string; // Защищено
  private isAdmin: boolean; // Защищено

  public changePassword(oldPass: string, newPass: string) {
    // Контролируемое изменение с проверками
  }
}
```

#### Поддерживаемость

```typescript
class DataProcessor {
  private rawData: any[];
  private temporaryCache: Map<string, any>;

  // Если решим изменить внутреннюю реализацию,
  // это не затронет внешний код
  public processData(): Result {
    this.validateData();
    this.cleanData();
    return this.analyzeData();
  }

  private validateData() {
    /* можно менять без последствий */
  }
  private cleanData() {
    /* можно менять без последствий */
  }
  private analyzeData() {
    /* можно менять без последствий */
  }
}
```

#### Четкие контракты

```typescript
class APIClient {
  // Public - это контракт, который мы обещаем соблюдать
  public fetchUsers(): Promise<User[]> {
    return this.makeRequest("/users");
  }

  // Private - внутренняя реализация, которую можем менять
  private async makeRequest(endpoint: string) {
    // Можем менять HTTP-библиотеку, логику повторов и т.д.
  }
}
```

### 5. Типичные сценарии для каждого модификатора

#### Private используйте для:

- Внутренних вспомогательных методов
- Чувствительных данных (пароли, ключи)
- Временных состояний
- Сложной внутренней логики

#### Protected используйте для:

- Свойств/методов для расширения в наследниках
- Общей логики для семейства классов
- Шаблонных методов (Template Method pattern)

#### Public используйте для:

- Публичного API класса
- Методов, которые должны вызываться извне
- Свойств, которые безопасно менять извне

### 6. Вопросы для самопроверки

При выборе модификатора задайте себе:

1. **"Кто должен иметь доступ к этому свойству/методу?"**
2. **"Что произойдет, если кто-то извне изменит это свойство?"**
3. **"Нужно ли это наследникам для расширения функциональности?"**
4. **"Является ли это частью обещанного внешнего интерфейса?"**

### 7. Исключения из правила

Иногда можно начинать с `public` для:

- Простых DTO (Data Transfer Objects)
- Конфигурационных объектов
- Классов-значений (Value Objects)

### Ключевые выводы:

1. **Консервативный подход** - private по умолчанию
2. **Ослабляйте постепенно** - только при явной необходимости
3. **Public = публичный контракт** - то, что вы обещаете поддерживать
4. **Безопасность и поддерживаемость** - главные преимущества подхода
5. **Сознательный выбор** - каждый модификатор должен быть обдуман

Эта стратегия поможет вам создавать более безопасные, надежные и поддерживаемые приложения! 🛡️

Запомните: **"Сначала спрячь, потом постепенно открывай"** - это золотое правило проектирования классов в TypeScript!
