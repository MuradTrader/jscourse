## Наследование в TypeScript классах

### 1. Что такое наследование?

**Наследование** — это механизм, при котором один класс (дочерний) получает свойства и методы другого класса (родительского).

**Аналогия:** Представьте, что `User` — это общее понятие "человек", а `Admin` — "администратор". Администратор — это тоже человек, но с дополнительными правами.

### 2. Терминология

- **Родительский класс (Parent Class)** / **Базовый класс (Base Class)** / **Суперкласс (Superclass)** — класс, от которого наследуют (`User`)
- **Дочерний класс (Child Class)** / **Производный класс (Derived Class)** / **Подкласс (Subclass)** — класс, который наследует (`Admin`)

### 3. Проблема дублирования кода

**Неправильный подход** — копирование кода:

```typescript
class Admin {
  name: string;
  email: string;
  lastName?: string;

  constructor(name: string, email: string, lastName?: string) {
    this.name = name;
    this.email = email;
    this.lastName = lastName;
  }

  greet() {
    return `Hello ${this.name}`;
  }

  // Дополнительные свойства Admin
  isAdmin: boolean = true;
}
```

**Проблемы этого подхода:**

- **Дублирование кода** - те же свойства, конструктор, методы
- **Сложность поддержки** - при изменении `User` нужно помнить изменить `Admin`
- **Ошибки** - легко забыть добавить новое свойство во все классы

### 4. Решение через наследование

Используем ключевое слово `extends`:

```typescript
class User {
  name: string;
  email: string;
  lastName?: string;

  constructor(name: string, email: string, lastName?: string) {
    this.name = name;
    this.email = email;
    this.lastName = lastName;
  }

  greet() {
    return `Hello ${this.name}`;
  }
}

class Admin extends User {
  isAdmin: boolean = true;
}
```

### 5. Что происходит при наследовании?

**Admin автоматически получает:**

- Все свойства: `name`, `email`, `lastName`
- Все методы: `greet()`
- Конструктор родительского класса

**Проверка в коде:**

```typescript
const admin = new Admin("Mark", "mark@email.com");
console.log(admin.name); // "Mark" - из User
console.log(admin.email); // "mark@email.com" - из User
console.log(admin.greet()); // "Hello Mark" - из User
console.log(admin.isAdmin); // true - из Admin
```

### 6. Важное ограничение TypeScript

**Одиночное наследование:** Класс может наследовать только от одного класса.

```typescript
// ❌ НЕ РАБОТАЕТ в TypeScript
class Admin extends User, AnotherClass { }

// ✅ Только так
class Admin extends User { }
```

**Почему такое ограничение?** Чтобы избежать проблем "ромбовидного наследования", когда класс наследует от двух классов с одинаковыми методами.

### 7. Конструктор в наследовании

**Важный момент:** Дочерний класс должен удовлетворять требованиям конструктора родительского класса.

```typescript
class Admin extends User {
  isAdmin: boolean = true;

  // Конструктор не обязателен, но если нужен - должен вызывать super()
}
```

**Если не передать обязательные параметры:**

```typescript
const admin = new Admin(); // ❌ ОШИБКА: Expected 2 arguments
```

**Правильное создание:**

```typescript
const admin = new Admin("Mark", "mark@email.com"); // ✅
```

### 8. Что видно в консоли

При выводе объектов в консоль:

- `user` показывается как `User`
- `admin` показывается как `Admin` (даже though он наследует от `User`)

```typescript
console.log(user); // User { name: "John", email: "john@email.com" }
console.log(admin); // Admin { name: "Mark", email: "mark@email.com", isAdmin: true }
```

### 9. Практическая польза наследования

**Гибкая архитектура приложения:**

```typescript
class User {
  /* базовые свойства */
}
class Admin extends User {
  /* права администратора */
}
class Customer extends User {
  /* свойства покупателя */
}
class Moderator extends User {
  /* права модератора */
}
```

### 10. Преимущества наследования

1. **Повторное использование кода** - не дублируем общую логику
2. **Легкость поддержки** - изменения в `User` автоматически применяются ко всем наследникам
3. **Четкая иерархия** - логическая структура "является" (Admin является User)
4. **Расширяемость** - можно добавлять новую функциональность в дочерние классы

### 11. Что будет дальше?

Автор намекает, что будут более глубокие темы наследования:

- Вызов конструктора родительского класса (`super()`)
- Переопределение методов
- Доступ к родительским методам через `super`

### Ключевые выводы:

1. **`extends`** - ключевое слово для наследования
2. **Одиночное наследование** - класс может наследовать только от одного класса
3. **Автоматическое наследование** - дочерний класс получает все свойства и методы родителя
4. **Собственные свойства** - дочерний класс может добавлять свои уникальные свойства
5. **Соблюдение контракта** - дочерний класс должен удовлетворять требованиям родительского конструктора

Наследование — это мощный инструмент для создания хорошо структурированных и поддерживаемых приложений! 🎯
