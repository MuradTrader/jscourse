## Дженерики в классах TypeScript

### 1. Проблема, которую решают дженерики

Автор начинает с создания класса `Box`, который может хранить любое значение:

```typescript
class Box {
  private _value: any; // Проблема: тип any

  constructor(value: any) {
    this._value = value;
  }

  get value(): any {
    return this._value;
  }

  set value(newValue: any) {
    this._value = newValue;
  }
}
```

**Проблемы подхода с `any`:**

- **Потеря типизации** - TypeScript не знает, какой тип хранится в Box
- **Нет проверки типов** - можно положить число, а потом заменить на строку
- **Нет автодополнения** - IDE не может подсказать методы для конкретного типа

### 2. Решение через дженерики

**Дженерики** позволяют создавать классы, которые работают с разными типами, но сохраняют информацию о конкретном типе.

#### Синтаксис дженериков в классах:

```typescript
class ClassName<T> {
  // T можно использовать как тип внутри класса
}
```

### 3. Преобразование класса Box с дженериком

```typescript
class Box<T> {
  private _value: T; // Теперь _value имеет тип T

  constructor(value: T) {
    this._value = value;
  }

  get value(): T {
    // Геттер возвращает T
    return this._value;
  }

  set value(newValue: T) {
    // Сеттер принимает T
    this._value = newValue;
  }
}
```

### 4. Как работают дженерики?

#### Автоматический вывод типа:

```typescript
// TypeScript автоматически определяет, что numberBox - это Box<number>
const numberBox = new Box(123);
// Эквивалентно: const numberBox = new Box<number>(123);

numberBox.value = 456; // ✅ OK - число
numberBox.value = "hello"; // ❌ ОШИБКА: Type 'string' is not assignable to type 'number'
```

#### Явное указание типа:

```typescript
// Можно явно указать тип
const stringBox = new Box<string>("hello");
stringBox.value = "world"; // ✅ OK - строка
stringBox.value = 123; // ❌ ОШИБКА: Type 'number' is not assignable to type 'string'
```

### 5. Детальный разбор кода автора

#### Исходный класс без дженериков:

```typescript
class Box {
  private _value: any;

  constructor(value: any) {
    this._value = value;
  }

  get value(): any {
    return this._value;
  }

  set value(newValue: any) {
    this._value = newValue;
  }
}
```

#### Проблема с типом `any`:

```typescript
const numberBox = new Box(123);
// TypeScript видит: const numberBox: Box
// НЕ ЗНАЕТ, что внутри число!

numberBox.value = "hello"; // ❌ Должна быть ошибка, но TypeScript разрешает
```

#### Класс с дженериком:

```typescript
class Box<T> {
  private _value: T;

  constructor(value: T) {
    this._value = value;
  }

  get value(): T {
    return this._value;
  }

  set value(newValue: T) {
    this._value = newValue;
  }
}
```

#### Результат с дженериком:

```typescript
const numberBox = new Box(123);
// TypeScript видит: const numberBox: Box<number>
// ТОЧНО ЗНАЕТ, что внутри число!

console.log(numberBox.value.toFixed(2)); // ✅ Автодополнение для чисел
numberBox.value = "hello"; // ❌ ОШИБКА: Нельзя присвоить строку
```

### 6. Преимущества дженериков

#### 1. Безопасность типов

```typescript
const numberBox = new Box<number>(123);
numberBox.value = "hello"; // ❌ Компилятор запрещает
```

#### 2. Автодополнение в IDE

```typescript
const stringBox = new Box("hello");
console.log(stringBox.value.length); // ✅ IDE подсказывает .length
console.log(stringBox.value.toFixed()); // ❌ IDE показывает ошибку
```

#### 3. Переиспользование кода

```typescript
// Один класс работает с разными типами
const numberBox = new Box(1);
const stringBox = new Box("text");
const booleanBox = new Box(true);
```

### 7. Синтаксические варианты

#### Автоматический вывод:

```typescript
const box = new Box("hello"); // Box<string>
```

#### Явное указание:

```typescript
const box = new Box<string>("hello"); // Box<string>
```

#### Избыточное явное указание:

```typescript
const box: Box<string> = new Box<string>("hello");
```

### 8. Ограничения и проверки типов

#### Строгая проверка при присваивании:

```typescript
const numberBox = new Box(123);

// ✅ Разрешенные операции:
numberBox.value = 456;
const currentValue: number = numberBox.value;

// ❌ Запрещенные операции:
numberBox.value = "text"; // string → number
numberBox.value = undefined; // undefined → number
const text: string = numberBox.value; // number → string
```

### 9. Более сложные примеры дженериков

#### Дженерик с несколькими типами:

```typescript
class Pair<T, U> {
  constructor(public first: T, public second: U) {}
}

const pair = new Pair("age", 25); // Pair<string, number>
```

#### Дженерик с ограничениями (constraints):

```typescript
interface HasLength {
  length: number;
}

class LengthBox<T extends HasLength> {
  constructor(private item: T) {}

  get length(): number {
    return this.item.length;
  }
}

const stringBox = new LengthBox("hello"); // ✅ OK - строка имеет length
const arrayBox = new LengthBox([1, 2, 3]); // ✅ OK - массив имеет length
const numberBox = new LengthBox(123); // ❌ ОШИБКА - число не имеет length
```

### 10. Сравнение подходов

#### Без дженериков (опасно):

```typescript
class Box {
  private _value: any;
  // Нет контроля типов, возможны runtime ошибки
}

const box = new Box(123);
box.value = "hello"; // Разрешено, но нелогично
console.log(box.value.toFixed()); // Runtime ошибка!
```

#### С дженериками (безопасно):

```typescript
class Box<T> {
  private _value: T;
  // Полный контроль типов, ошибки на этапе компиляции
}

const box = new Box(123);
box.value = "hello"; // Ошибка компиляции
console.log(box.value.toFixed()); // Безопасно - всегда число
```

### 11. Практическое применение

#### Контейнеры данных:

```typescript
class Stack<T> {
  private items: T[] = [];

  push(item: T): void {
    this.items.push(item);
  }

  pop(): T | undefined {
    return this.items.pop();
  }
}

const numberStack = new Stack<number>();
numberStack.push(1);
numberStack.push(2);
const num = numberStack.pop(); // number | undefined

const stringStack = new Stack<string>();
stringStack.push("hello");
```

#### Репозитории:

```typescript
interface Entity {
  id: number;
}

class Repository<T extends Entity> {
  private entities: T[] = [];

  findById(id: number): T | undefined {
    return this.entities.find((entity) => entity.id === id);
  }

  save(entity: T): void {
    this.entities.push(entity);
  }
}

class User implements Entity {
  constructor(public id: number, public name: string) {}
}

const userRepository = new Repository<User>();
userRepository.save(new User(1, "John"));
const user = userRepository.findById(1); // User | undefined
```

### Ключевые выводы:

1. **Дженерики = параметризация типов** - класс становится шаблоном
2. **`class ClassName<T>`** - синтаксис объявления дженерика
3. **Безопасность типов** - ошибки обнаруживаются на этапе компиляции
4. **Автодополнение** - IDE понимает конкретный тип
5. **Переиспользование** - один класс для многих типов

Дженерики превращают классы из статических шаблонов в динамические, типобезопасные конструкции! 🎯
