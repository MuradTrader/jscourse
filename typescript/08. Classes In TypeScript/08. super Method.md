## Конструкторы в наследовании и вызов super()

### 1. Проблема статичности дочернего класса

В предыдущей лекции класс `Admin` был статическим:

```typescript
class Admin extends User {
  isAdmin: boolean = true; // Всегда true, нет динамики
}
```

Теперь мы хотим добавить динамическое свойство `usersReporting` - количество пользователей, которые подчиняются администратору.

### 2. Добавление динамического свойства

```typescript
class Admin extends User {
  isAdmin: boolean = true;
  usersReporting: number; // ОШИБКА: Property 'usersReporting' has no initializer
}
```

**Проблема:** TypeScript требует инициализации свойства, но мы хотим задавать значение динамически при создании объекта.

### 3. Попытка добавить конструктор

```typescript
class Admin extends User {
  isAdmin: boolean = true;
  usersReporting: number;

  constructor(usersReporting: number) {
    this.usersReporting = usersReporting; // ОШИБКА!
  }
}
```

**Ошибка:** "Constructors for derived classes must contain a 'super' call"

### 4. Ключевое слово `super`

#### Что такое `super`?

- `super` - это вызов конструктора родительского класса
- **Должен быть первым** оператором в конструкторе дочернего класса
- Обеспечивает инициализацию родительской части объекта до дочерней

#### Почему это важно?

```typescript
class Admin extends User {
  // Admin имеет доступ к свойствам User: name, email, lastName
  // Но эти свойства должны быть сначала инициализированы в User!
}
```

### 5. Правильная реализация конструктора

```typescript
class Admin extends User {
  isAdmin: boolean = true;
  usersReporting: number;

  constructor(
    name: string,
    email: string,
    usersReporting: number,
    lastName?: string
  ) {
    super(name, email, lastName); // Вызов конструктора User
    this.usersReporting = usersReporting; // Теперь можно использовать this
  }
}
```

### 6. Анализ сигнатуры super()

Конструктор `User` ожидает:

```typescript
constructor(name: string, email: string, lastName?: string)
```

Поэтому `super()` должен получить те же аргументы:

- `name` (обязательный)
- `email` (обязательный)
- `lastName` (необязательный)

### 7. Порядок параметров в конструкторе

**Правильный порядок:**

```typescript
constructor(
    name: string,        // Обязательные из родителя
    email: string,       // Обязательные из родителя
    usersReporting: number, // Обязательные дочерние
    lastName?: string    // Необязательные из родителя
)
```

**Неправильный порядок:**

```typescript
constructor(
    name: string,
    email: string,
    lastName?: string,   // Необязательный ПЕРЕД обязательным
    usersReporting: number // ОШИБКА!
)
```

### 8. Что происходит при создании Admin

```typescript
const admin = new Admin("Mark", "mark@email.com", 11);
```

**Процесс:**

1. Вызывается `constructor("Mark", "mark@email.com", 11)`
2. Вызывается `super("Mark", "mark@email.com", undefined)` - создается User часть
3. Выполняется `this.usersReporting = 11` - добавляется Admin часть

### 9. Гибкость super() - статические значения

Автор показывает важную особенность - в `super()` можно передавать не только параметры конструктора, но и статические значения:

```typescript
constructor(name: string, email: string, usersReporting: number) {
    super(name, email, "Admin"); // Все админы получат lastName = "Admin"
    this.usersReporting = usersReporting;
}
```

**Или даже:**

```typescript
constructor(usersReporting: number) {
    super("Default Admin", "admin@system.com"); // Статические значения
    this.usersReporting = usersReporting;
}
```

### 10. Логика в super()

В `super()` можно передавать результаты вычислений:

```typescript
constructor(name: string, email: string, usersReporting: number) {
    super(name.toUpperCase(), email, "Administrator");
    this.usersReporting = usersReporting;
}
```

### 11. Практический пример

```typescript
class User {
  name: string;
  email: string;
  lastName?: string;

  constructor(name: string, email: string, lastName?: string) {
    this.name = name;
    this.email = email;
    this.lastName = lastName;
  }

  greet() {
    return `Hello ${this.name}`;
  }
}

class Admin extends User {
  isAdmin: boolean = true;
  usersReporting: number;

  constructor(
    name: string,
    email: string,
    usersReporting: number,
    lastName?: string
  ) {
    super(name, email, lastName);
    this.usersReporting = usersReporting;
  }
}

// Использование:
const admin1 = new Admin("Mark", "mark@email.com", 11);
const admin2 = new Admin("Sarah", "sarah@company.com", 5, "Johnson");

console.log(admin1);
// Admin { name: "Mark", email: "mark@email.com", usersReporting: 11, isAdmin: true }

console.log(admin2);
// Admin { name: "Sarah", email: "sarah@company.com", lastName: "Johnson", usersReporting: 5, isAdmin: true }
```

### 12. Ошибки, которых теперь можно избежать

**TypeScript предотвращает:**

- Создание Admin без обязательных параметров User
- Нарушение порядка вызова (super() не первый)
- Использование this до вызова super()

### Ключевые выводы:

1. **super() должен быть первым** в конструкторе дочернего класса
2. **super() вызывает конструктор родителя** с нужными аргументами
3. **Порядок параметров важен** - сначала обязательные, потом необязательные
4. **super() может принимать** как параметры конструктора, так и статические значения
5. **TypeScript строго следит** за правильностью вызова super()

Эта концепция критически важна для построения сложных иерархий классов в TypeScript! 🎯
