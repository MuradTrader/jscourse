## Protected модификатор доступа - практическое применение

### 1. Теоретическое напоминание

**Protected модификатор** — средний уровень доступа:

- ✅ Доступен внутри класса
- ✅ Доступен в дочерних классах
- ❌ Недоступен в экземплярах класса (объектах)

### 2. Добавление protected свойства

Автор добавляет protected свойство `phone` в класс `User`:

```typescript
class User {
  public name: string;
  public email: string;
  protected phone: number; // Protected свойство

  constructor(name: string, email: string, phone: number) {
    this.name = name;
    this.email = email;
    this.phone = phone; // Инициализация в конструкторе
  }
}
```

### 3. Доступ к protected свойству в родительском классе

Protected свойства доступны внутри того же класса:

```typescript
class User {
  protected phone: number;

  public getFormattedPhone() {
    // ✅ Доступ внутри класса
    return `Phone: ${this.phone}`;
  }
}
```

### 4. Доступ к protected свойству в дочернем классе

**Ключевой момент:** Protected свойства доступны в дочерних классах:

```typescript
class Admin extends User {
  public printPhone() {
    // ✅ Доступ в дочернем классе
    console.log(this.phone);
  }
}
```

### 5. Обновление конструктора дочернего класса

При добавлении protected свойства нужно обновить конструктор:

```typescript
class Admin extends User {
  constructor(
    name: string,
    email: string,
    phone: number,
    usersReporting: number
  ) {
    super(name, email, phone); // Передаем phone в родительский конструктор
    this.usersReporting = usersReporting;
  }
}
```

### 6. Попытка доступа из экземпляра - ОШИБКА

**Главное отличие от public:** Protected свойства недоступны из экземпляров:

```typescript
const user = new User("John", "john@email.com", 123456789);
const admin = new Admin("Mark", "mark@email.com", 987654321, 11);

console.log(user.phone); // ❌ ОШИБКА: Property 'phone' is protected
console.log(admin.phone); // ❌ ОШИБКА: Property 'phone' is protected
```

### 7. Важная настройка TypeScript: noEmitOnError

Автор показывает критически важную настройку в `tsconfig.json`:

```json
{
  "compilerOptions": {
    "noEmitOnError": true
  }
}
```

**Что это значит:**

- `noEmitOnError: false` (по умолчанию) - TypeScript компилирует в JS даже с ошибками
- `noEmitOnError: true` - TypeScript НЕ компилирует в JS при наличии ошибок

**Почему это важно:**

- Без этой настройки protected свойства все равно доступны в скомпилированном JavaScript
- С этой настройкой TypeScript строго соблюдает правила доступа

### 8. Protected методы

Модификатор `protected` применяется и к методам:

```typescript
class User {
    protected validatePhone(phone: number) {
        // Логика проверки телефона
        return phone > 0;
    }
}

class Admin extends User {
    public updatePhone(newPhone: number) {
        if (this.validatePhone(newPhone)) { // ✅ Доступ к protected методу
            this.phone = newPhone;
        }
    }
}

const admin = new Admin(...);
admin.validatePhone(123); // ❌ ОШИБКА: Method 'validatePhone' is protected
```

### 9. Обходной путь - public методы, использующие protected

Можно создавать public методы, которые внутри используют protected функциональность:

```typescript
class Admin extends User {
    protected printPhone() {
        console.log(this.phone);
    }

    public useProtectedPhone() {
        // ✅ Вызов protected метода из public метода
        this.printPhone();
    }
}

const admin = new Admin(...);
admin.useProtectedPhone(); // ✅ Работает - выводит телефон
```

### 10. Практический пример автора

```typescript
class User {
  public name: string;
  protected phone: number;

  constructor(name: string, phone: number) {
    this.name = name;
    this.phone = phone;
  }
}

class Admin extends User {
  constructor(name: string, phone: number) {
    super(name, phone);
  }

  public printPhone() {
    console.log(this.phone); // ✅ Доступ в дочернем классе
  }
}

// Использование:
const admin = new Admin("Mark", 123456789);

console.log(admin.name); // ✅ "Mark" - public свойство
// console.log(admin.phone); // ❌ Ошибка - protected свойство
admin.printPhone(); // ✅ 123456789 - через public метод
```

### 11. Когда использовать protected?

**Используйте protected для:**

- Внутренней логики, которая нужна классу и его наследникам
- Данных, которые не должны изменяться извне, но нужны в иерархии классов
- Методов-помощников для наследников

**Практические примеры:**

```typescript
class BankAccount {
  protected accountId: string; // ID счета - для внутреннего использования
  protected calculateInterest() {} // Расчет процентов - внутренняя логика
  public getBalance() {} // Публичный интерфейс
}

class SavingsAccount extends BankAccount {
  public applyInterest() {
    this.calculateInterest(); // ✅ Используем protected метод родителя
  }
}
```

### 12. Сравнение public vs protected

| Контекст               | Public | Protected |
| ---------------------- | ------ | --------- |
| Внутри класса          | ✅     | ✅        |
| В дочерних классах     | ✅     | ✅        |
| В экземплярах объектов | ✅     | ❌        |

### Ключевые выводы:

1. **Protected = семейный доступ** - только для класса и его потомков
2. **Недоступен извне** - нельзя обратиться через экземпляр объекта
3. **noEmitOnError: true** - критически важно для строгой проверки
4. **Для методов тоже работает** - protected методы следуют тем же правилам
5. **Публичные врапперы** - можно создавать public методы, использующие protected функциональность

Protected модификатор идеально подходит для создания семейств классов, где нужен общий доступ к некоторой функциональности, но без экспозиции наружу! 🛡️
