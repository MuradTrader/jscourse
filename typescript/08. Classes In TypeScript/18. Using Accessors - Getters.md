## Геттеры (аксессоры) в TypeScript - контролируемое чтение свойств

### 1. Проблема, которую решают геттеры

Автор напоминает, что после создания сеттера для возраста у нас осталась проблема:

- Можно установить возраст через `john.age = 45`
- Но нельзя прочитать: `console.log(john.age)` возвращает `undefined`

### 2. Что такое геттер (аксессор)?

**Геттер** — это специальный метод, который выглядит и используется как свойство для чтения, но содержит логику, выполняемую при попытке получить значение.

**Ключевая особенность:** Вы обращаетесь к геттеру как к обычному свойству (без скобок!), но на самом деле выполняется функция.

### 3. Синтаксис объявления геттера

```typescript
class Person {
  private _age?: number;

  // Геттер для возраста
  public get age(): number {
    if (this._age === undefined) {
      throw new Error("Age property is not defined");
    }
    return this._age;
  }
}
```

### 4. Детальный разбор кода автора

#### Шаг 1: Создание геттера для возраста

```typescript
public get age(): number {
    if (this._age === undefined) {
        throw new Error("Age property is not defined");
    }
    return this._age;
}
```

**Разбор синтаксиса:**

- `public` - геттер доступен извне класса
- `get` - ключевое слово для объявления геттера
- `age` - имя свойства, которое будут использовать извне
- `(): number` - тип возвращаемого значения (без параметров!)
- **Геттеры не могут иметь параметров** - они только возвращают значение

#### Шаг 2: Важность проверки на undefined

```typescript
if (this._age === undefined) {
  throw new Error("Age property is not defined");
}
```

**Почему это важно:**

- `_age` объявлен как `number | undefined` (из-за `?`)
- Если пользователь не установил возраст, геттер должен сообщить об ошибке
- Это защита от использования неинициализированного значения

### 5. Как использовать геттер и сеттер вместе?

**Теперь полный цикл работы с возрастом:**

```typescript
const john = new Person("John", "Doe");

// Установка через сеттер
john.age = 45; // Вызывается сеттер age(45)

// Чтение через геттер
console.log(john.age); // Вызывается геттер age() -> 45
```

**Магия:** Оба вызова выглядят как работа с обычным свойством!

### 6. Решение проблемы fullName через геттер

#### Было (неудобный метод):

```typescript
public fullName(): string {
    return `${this.firstName} ${this.lastName}`;
}

// Использование:
console.log(john.fullName()); // Нужно вызывать как метод
```

#### Стало (элегантный геттер):

```typescript
public get fullName(): string {
    return `${this.firstName} ${this.lastName}`;
}

// Использование:
console.log(john.fullName); // Используется как свойство!
```

### 7. Преимущества геттера для fullName

#### Автоматическое обновление:

```typescript
const person = new Person("John", "Doe");
console.log(person.fullName); // "John Doe"

person.firstName = "Mike";
console.log(person.fullName); // "Mike Doe" - АВТОМАТИЧЕСКИ обновилось!
```

#### Естественный интерфейс:

```typescript
// Интуитивно понятно - fullName это свойство, а не действие
const name = person.fullName; // Вместо person.getFullName()
```

### 8. Полный итоговый код класса Person

```typescript
class Person {
  private _age?: number;

  constructor(public firstName: string, public lastName: string) {}

  // Сеттер для возраста с валидацией
  public set age(age: number) {
    if (age > 200 || age < 0) {
      throw new Error("The age must be within the age range 0 to 200");
    }
    this._age = age;
  }

  // Геттер для возраста с проверкой инициализации
  public get age(): number {
    if (this._age === undefined) {
      throw new Error("Age property is not defined");
    }
    return this._age;
  }

  // Геттер для полного имени (вычисляемое свойство)
  public get fullName(): string {
    return `${this.firstName} ${this.lastName}`;
  }
}
```

### 9. Примеры использования

#### ✅ Корректное использование:

```typescript
const john = new Person("John", "Doe");
john.age = 25; // Сеттер
console.log(john.age); // Геттер -> 25
console.log(john.fullName); // Геттер -> "John Doe"
```

#### ❌ Ошибки:

```typescript
const john = new Person("John", "Doe");
console.log(john.age); // Ошибка: Age property is not defined

john.age = 300; // Ошибка: The age must be within the age range 0 to 200
```

### 10. Важные особенности геттеров и сеттеров

#### Ограничения параметров:

- **Геттер не может иметь параметров** - только возвращает значение
- **Сеттер может иметь только один параметр** - значение для установки

#### Совместное использование:

```typescript
// Для одного свойства можно иметь и геттер, и сеттер
public set age(value: number) { /* логика установки */ }
public get age(): number { /* логика получения */ }
```

#### Не обязаны работать с приватными свойствами:

```typescript
class Person {
  constructor(public firstName: string) {}

  // Геттер может работать с публичными свойствами
  public get upperCaseName(): string {
    return this.firstName.toUpperCase();
  }
}
```

### 11. Когда использовать геттеры и сеттеры?

#### Используйте геттеры когда:

- Нужно предоставить вычисляемое свойство (как `fullName`)
- Нужно лениво вычислять значение
- Нужно контролировать доступ к чтению свойства

#### Используйте сеттеры когда:

- Нужно валидировать значение перед установкой
- Нужно выполнить дополнительные действия при изменении значения
- Нужно контролировать доступ к записи свойства

### 12. Практические сценарии

#### Сценарий 1: Валидация email

```typescript
class User {
  private _email: string = "";

  public set email(value: string) {
    if (!value.includes("@")) {
      throw new Error("Invalid email address");
    }
    this._email = value;
  }

  public get email(): string {
    return this._email;
  }
}
```

#### Сценарий 2: Вычисляемое свойство на основе других свойств

```typescript
class Rectangle {
  constructor(public width: number, public height: number) {}

  public get area(): number {
    return this.width * this.height;
  }

  public get perimeter(): number {
    return 2 * (this.width + this.height);
  }
}
```

### Ключевые выводы:

1. **Геттер = умное чтение значения** - выглядит как свойство, работает как метод
2. **Синтаксис `get propertyName()`** - ключевое слово `get`, без параметров
3. **Использование как обычного свойства** - `obj.property` (без скобок!)
4. **Идеально для вычисляемых свойств** - как `fullName`
5. **Работают в паре с сеттерами** - для полного контроля над свойством

Геттеры и сеттеры позволяют создавать элегантные и безопасные API для классов, сочетающие простоту использования с мощной логикой! 🎯

Теперь у вас есть полный арсенал для создания "умных" свойств в TypeScript!
