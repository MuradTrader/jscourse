## Расширение интерфейсов и создание специализированных репозиториев

### 1. Улучшение архитектуры через наследование типов

Автор показывает, как избежать дублирования кода через расширение интерфейса `Identifiable`:

#### Было (дублирование):

```typescript
interface Identifiable {
  id: number;
}

type User = {
  id: number; // Дублируется
  name: string;
  email: string;
};
```

#### Стало (наследование):

```typescript
interface Identifiable {
  id: number;
}

type User = Identifiable & {
  name: string;
  email: string;
};
```

**Альтернативный синтаксис:**

```typescript
// Через extends (для интерфейсов)
interface User extends Identifiable {
  name: string;
  email: string;
}

// Через пересечение типов (для type)
type User = Identifiable & {
  name: string;
  email: string;
};
```

### 2. Создание типа Book

Автор создает второй тип для демонстрации гибкости репозитория:

```typescript
type Book = Identifiable & {
  title: string;
  isbn: number;
};
```

**Что это дает:**

- `Book` автоматически получает свойство `id` от `Identifiable`
- Гарантируется, что все книги имеют обязательные свойства
- Легко добавлять новые типы сущностей

### 3. Создание специализированных репозиториев

#### Репозиторий пользователей:

```typescript
const usersRepository = new Repository<User>();
```

#### Репозиторий книг:

```typescript
const booksRepository = new Repository<Book>();
```

### 4. Демонстрация строгой типобезопасности

#### ✅ Корректное использование:

```typescript
// Добавление пользователя
usersRepository.add({
  id: 1,
  name: "John",
  email: "john@email.com",
});

// Добавление книги
booksRepository.add({
  id: 1,
  title: "Harry Potter",
  isbn: 1234567890,
});
```

#### ❌ Некорректное использование (ошибки TypeScript):

```typescript
// Попытка добавить пользователя в репозиторий книг
booksRepository.add({
  id: 2,
  name: "Alice", // ❌ ОШИБКА: свойство 'name' не существует в 'Book'
  email: "alice@email.com", // ❌ ОШИБКА: свойство 'email' не существует в 'Book'
});

// Попытка добавить книгу в репозиторий пользователей
usersRepository.add({
  id: 2,
  title: "TypeScript Guide", // ❌ ОШИБКА: свойство 'title' не существует в 'User'
  isbn: 987654321, // ❌ ОШИБКА: свойство 'isbn' не существует в 'User'
});
```

### 5. Использование методов репозитория

#### Получение всех элементов:

```typescript
const allUsers = usersRepository.getAll();
// TypeScript знает: const allUsers: User[]

const allBooks = booksRepository.getAll();
// TypeScript знает: const allBooks: Book[]
```

#### Автодополнение в IDE:

```typescript
// Для пользователей
const user = usersRepository.getById(1);
if (user) {
  console.log(user.name); // ✅ Автодополнение: name, email, id
  console.log(user.email); // ✅
  // console.log(user.title); // ❌ Ошибка - нет свойства title
}

// Для книг
const book = booksRepository.getById(1);
if (book) {
  console.log(book.title); // ✅ Автодополнение: title, isbn, id
  console.log(book.isbn); // ✅
  // console.log(book.name); // ❌ Ошибка - нет свойства name
}
```

### 6. Визуализация того, что происходит

#### Во время компиляции TypeScript "видит":

```typescript
// usersRepository - это фактически:
class Repository<User> {
  private items: User[] = [];

  add(item: User): void {
    /* ... */
  }
  getById(id: number): User | undefined {
    /* ... */
  }
  getAll(): User[] {
    /* ... */
  }
}

// booksRepository - это фактически:
class Repository<Book> {
  private items: Book[] = [];

  add(item: Book): void {
    /* ... */
  }
  getById(id: number): Book | undefined {
    /* ... */
  }
  getAll(): Book[] {
    /* ... */
  }
}
```

### 7. Преимущества такого подхода

#### 1. Единая кодовая база

```typescript
// Один класс Repository заменяет:
class UserRepository {
  /* методы для User */
}
class BookRepository {
  /* методы для Book */
}
class ProductRepository {
  /* методы для Product */
}
// ... и так для каждой сущности
```

#### 2. Легкость добавления новых типов

```typescript
// Добавляем новый тип - не нужно создавать новый класс репозитория
type Product = Identifiable & {
  name: string;
  price: number;
  category: string;
};

const productsRepository = new Repository<Product>(); // ✅ Готово!
```

#### 3. Строгая типобезопасность

```typescript
// TypeScript предотвращает ошибки на этапе компиляции
usersRepository.add({
  id: 3,
  name: "Bob",
  // email: "bob@email.com" // ❌ Ошибка: отсутствует обязательное свойство
});
```

### 8. Расширенная демонстрация работы

#### Пример с несколькими сущностями:

```typescript
// Определяем несколько типов
type User = Identifiable & { name: string; email: string };
type Book = Identifiable & { title: string; isbn: number };
type Product = Identifiable & { name: string; price: number };

// Создаем репозитории для каждого типа
const usersRepo = new Repository<User>();
const booksRepo = new Repository<Book>();
const productsRepo = new Repository<Product>();

// Каждый репозиторий работает только со своим типом
usersRepo.add({ id: 1, name: "John", email: "john@test.com" });
booksRepo.add({ id: 1, title: "TypeScript", isbn: 123456 });
productsRepo.add({ id: 1, name: "Laptop", price: 999 });

// TypeScript обеспечивает безопасность
const user = usersRepo.getById(1); // User | undefined
const book = booksRepo.getById(1); // Book | undefined
const product = productsRepo.getById(1); // Product | undefined
```

### 9. Промышленное применение

Такой подход используется в реальных ORM и системах управления данными:

```typescript
// Аналогично TypeORM, Prisma, и другим библиотекам
const userRepository = connection.getRepository(User);
const bookRepository = connection.getRepository(Book);

// Каждый репозиторий имеет типобезопасные методы
await userRepository.find({ where: { name: "John" } });
await bookRepository.save(new Book());
```

### 10. Возможные расширения

#### Добавление специализированных методов:

```typescript
class UserRepository extends Repository<User> {
  findByEmail(email: string): User | undefined {
    return this.getAll().find((user) => user.email === email);
  }
}

class BookRepository extends Repository<Book> {
  findByTitle(title: string): Book | undefined {
    return this.getAll().find((book) => book.title.includes(title));
  }
}
```

### Ключевые выводы:

1. **Наследование типов** - избегаем дублирования через `Identifiable &`
2. **Один класс - множество типов** - дженерики создают специализированные репозитории
3. **Строгая изоляция** - нельзя смешивать типы в репозиториях
4. **Полная типобезопасность** - ошибки обнаруживаются при компиляции
5. **Промышленные паттерны** - такой подход используется в реальных ORM

Эта демонстрация показывает, как дженерики превращают абстрактные классы в мощные, типобезопасные инструменты для работы с разнородными данными! 🚀

Гибкость + безопасность типов = идеальная комбинация для enterprise-приложений!
