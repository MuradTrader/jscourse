## Статические члены класса в TypeScript

### 1. Что такое статические члены?

**Статические члены** — это свойства и методы, которые принадлежат самому классу, а не его экземплярам (объектам).

**Ключевое отличие от обычных членов:**

- **Обычные члены** - принадлежат каждому объекту, созданному из класса
- **Статические члены** - принадлежат самому классу как единому целому

### 2. Синтаксис объявления статических членов

```typescript
class Counter {
  // Статическое свойство
  static count: number = 0;

  // Статический метод
  static increment(): void {
    Counter.count += 1;
  }
}
```

### 3. Детальный разбор примера автора

#### Класс Counter со статическими членами:

```typescript
class Counter {
  // Статическое свойство - принадлежит классу Counter
  static count: number = 0;

  // Статический метод - принадлежит классу Counter
  static increment(): void {
    Counter.count += 1;
  }
}
```

### 4. Как использовать статические члены?

#### Доступ через имя класса (без создания объектов):

```typescript
// Доступ к статическому свойству
console.log(Counter.count); // 0

// Вызов статического метода
Counter.increment();
console.log(Counter.count); // 1

Counter.increment();
console.log(Counter.count); // 2
```

**Важно:** Не нужно создавать объекты через `new Counter()`!

### 5. Ключевые особенности статических членов

#### 1. Принадлежность классу, а не объектам

```typescript
const counter1 = new Counter();
const counter2 = new Counter();

// ❌ НЕ РАБОТАЕТ - статические члены недоступны через экземпляры
console.log(counter1.count); // Ошибка!
counter1.increment(); // Ошибка!

// ✅ РАБОТАЕТ - только через класс
console.log(Counter.count); // 2
Counter.increment(); // Увеличивает счетчик
```

#### 2. Общие данные для всех

```typescript
class Counter {
  static count: number = 0;
  static increment() {
    Counter.count++;
  }
}

// Все вызовы работают с ОДНИМ И ТЕМ ЖЕ счетчиком
Counter.increment(); // count = 1
Counter.increment(); // count = 2
Counter.increment(); // count = 3

// Где бы в коде вы ни обратились к Counter.count - получите 3
```

#### 3. Доступ через имя класса внутри статических методов

```typescript
class Counter {
  static count: number = 0;

  static increment() {
    // Используем Counter.count, а не this.count
    Counter.count += 1;
  }
}
```

### 6. Сравнение статических и обычных членов

| Аспект             | Обычные члены                | Статические члены           |
| ------------------ | ---------------------------- | --------------------------- |
| **Принадлежность** | Объектам класса              | Самому классу               |
| **Доступ**         | Через объекты: `obj.member`  | Через класс: `Class.member` |
| **Память**         | У каждого объекта своя копия | Одна копия на весь класс    |
| **Ключевое слово** | Без `static`                 | С `static`                  |

### 7. Практический пример автора

```typescript
class Counter {
  static count: number = 0;

  static increment(): void {
    Counter.count += 1;
  }
}

// Использование:
console.log(Counter.count); // 0 (начальное значение)

Counter.increment();
console.log(Counter.count); // 1

Counter.increment();
console.log(Counter.count); // 2

// Попытка использования через экземпляр - ОШИБКА:
const myCounter = new Counter();
// console.log(myCounter.count); // ❌ Property 'count' does not exist
// myCounter.increment();        // ❌ Property 'increment' does not exist
```

### 8. Почему использовать Counter.count вместо this.count?

В статических методах `this` ведет себя по-другому:

```typescript
class Counter {
  static count: number = 0;

  static increment() {
    // ❌ Небезопасно - this может быть переопределен
    this.count += 1;

    // ✅ Безопасно - явно указываем класс
    Counter.count += 1;
  }
}
```

### 9. Типичные сценарии использования статических членов

#### 1. Утилитные функции (самый частый случай)

```typescript
class MathUtils {
  static PI: number = 3.14159;

  static calculateCircleArea(radius: number): number {
    return this.PI * radius * radius;
  }

  static max(a: number, b: number): number {
    return a > b ? a : b;
  }
}

// Использование без создания объектов:
console.log(MathUtils.PI); // 3.14159
console.log(MathUtils.calculateCircleArea(5)); // 78.53975
console.log(MathUtils.max(10, 20)); // 20
```

#### 2. Счетчики и общее состояние

```typescript
class User {
  static userCount: number = 0;

  constructor(public name: string) {
    User.userCount++; // Увеличиваем счетчик при каждом новом пользователе
  }

  static getTotalUsers(): number {
    return User.userCount;
  }
}

const user1 = new User("John");
const user2 = new User("Jane");
console.log(User.getTotalUsers()); // 2
```

#### 3. Конфигурации и настройки

```typescript
class AppConfig {
  static readonly API_BASE_URL: string = "https://api.example.com";
  static readonly MAX_RETRIES: number = 3;
  static readonly TIMEOUT: number = 5000;
}

// Использование в любом месте приложения:
fetch(`${AppConfig.API_BASE_URL}/users`, {
  timeout: AppConfig.TIMEOUT,
});
```

### 10. Комбинация статических и обычных членов

Класс может содержать и статические, и обычные члены:

```typescript
class Car {
  // Статические члены - общие для всех машин
  static totalCars: number = 0;
  static getTotalCars() {
    return Car.totalCars;
  }

  // Обычные члены - уникальные для каждой машины
  constructor(public model: string) {
    Car.totalCars++;
  }
}

const car1 = new Car("Toyota");
const car2 = new Car("Honda");
console.log(Car.getTotalCars()); // 2 - статический метод
console.log(car1.model); // "Toyota" - обычное свойство
```

### 11. Ограничения статических членов

1. **Нет доступа к нестатическим членам** из статических методов
2. **Нельзя использовать `this`** для доступа к нестатическим членам
3. **Не наследуются** так же, как обычные члены (есть особенности)

### Ключевые выводы:

1. **`static` = принадлежность классу** - а не объектам
2. **Доступ через `ClassName.member`** - без создания экземпляров
3. **Идеально для утилит** - общих функций и констант
4. **Общее состояние** - один экземпляр на весь класс
5. **Не доступны через объекты** - только через класс

Статические члены — это мощный инструмент для создания утилитных классов и управления общим состоянием! 🎯

Готовы к более глубокому погружению в статические члены в следующем видео?
