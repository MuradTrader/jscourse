## Решение проблемы динамических полей класса

### 1. Проблема предыдущей реализации

В прошлой лекции у нас был конструктор с параметрами, но поля класса оставались статическими:

```typescript
class User {
  name = "John"; // Всегда "John"
  email = "john@email.com"; // Всегда "john@email.com"

  constructor(name: string, email: string) {
    // Параметры есть, но не присваиваются полям!
    console.log(name); // Выводим, но не сохраняем
  }
}
```

### 2. Ключевое слово `this`

**`this`** - это специальное ключевое слово, которое:

- Ссылается на текущий экземпляр класса
- Позволяет обращаться к полям и методам класса изнутри

```typescript
class User {
  name: string; // Объявляем поле без значения
  email: string; // Объявляем поле без значения

  constructor(name: string, email: string) {
    this.name = name; // Присваиваем параметр полю класса
    this.email = email; // Присваиваем параметр полю класса
  }
}
```

**Как это работает:**

- `this.name` - поле `name` текущего объекта
- `name` - параметр конструктора
- `this.name = name` - присваиваем значение параметра полю объекта

### 3. Удаление статических значений

До:

```typescript
name = "John"; // Статическое значение
email = "john@email.com"; // Статическое значение
```

После:

```typescript
name: string; // Только объявление типа
email: string; // Только объявление типа
```

### 4. Ошибка TypeScript и ее решение

**Ошибка:** "Property 'email' has no initializer and is not definitely assigned in the constructor"

**Причина:** TypeScript видит, что поле объявлено, но не инициализировано в конструкторе.

**Решение:** Присвоить значения всем полям в конструкторе:

```typescript
constructor(name: string, email: string) {
    this.name = name;    // Решаем проблему для name
    this.email = email;  // Решаем проблему для email
}
```

### 5. Сделаем метод `greet` динамическим

Было:

```typescript
greet() {
    return "Hello John"; // Всегда "John"
}
```

Стало:

```typescript
greet() {
    return `Hello ${this.name}`; // Динамическое имя
}
```

**Как работает:**

- `this.name` обращается к полю `name` текущего объекта
- Для `user1` вернет "Hello John"
- Для `user2` вернет "Hello Mark"

### 6. Проверка TypeScript

TypeScript теперь гарантирует:

- При создании объекта 必须传递 оба аргумента
- Оба аргумента должны быть строками

```typescript
// ОШИБКА: Expected 2 arguments
const user = new User("John");

// ОШИБКА: Argument of type 'number' is not assignable to parameter of type 'string'
const user = new User("John", 123);

// ПРАВИЛЬНО
const user = new User("John", "john@email.com");
```

### 7. Итоговый код

```typescript
class User {
  name: string;
  email: string;

  constructor(name: string, email: string) {
    this.name = name;
    this.email = email;
  }

  greet() {
    return `Hello ${this.name}`;
  }
}

// Использование:
const user1 = new User("John", "john@email.com");
const user2 = new User("Mark", "mark@email.com");

console.log(user1.greet()); // "Hello John"
console.log(user2.greet()); // "Hello Mark"
console.log(user1.name); // "John"
console.log(user2.name); // "Mark"
```

### Ключевые концепции, которые нужно запомнить:

1. **`this`** - ссылается на текущий экземпляр класса
2. **Присваивание в конструкторе** - `this.поле = параметр`
3. **Объявление полей без значений** - только с типами: `name: string`
4. **TypeScript проверяет инициализацию** - все поля должны быть проинициализированы
5. **Динамические методы** - используют `this` для доступа к полям объекта

### Почему это важно:

Теперь класс `User` стал по-настоящему переиспользуемым шаблоном! Вы можете:

- Создавать любое количество пользователей
- Каждый пользователь имеет уникальные данные
- TypeScript обеспечивает безопасность типов

Класс готов к использованию в реальных проектах! 🎉
