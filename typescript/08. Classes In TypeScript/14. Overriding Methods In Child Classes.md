## Переопределение методов в дочерних классах

### 1. Концепция переопределения методов

**Переопределение метода** — это возможность дочернего класса предоставить свою собственную реализацию метода, который уже существует в родительском классе.

**Аналогия:** Представьте, что класс `User` имеет метод `greet()`, который говорит "Привет". Класс `Admin` (наследник `User`) может переопределить этот метод, чтобы говорить "Привет, я администратор".

### 2. Основное правило переопределения

**Совместимость сигнатур методов:** При переопределении метода в дочернем классе его сигнатура должна точно совпадать с сигнатурой метода в родительском классе.

**Сигнатура метода включает:**

- Имя метода
- Типы и количество параметров
- Тип возвращаемого значения

### 3. Практический пример автора

#### Родительский класс:

```typescript
class User {
  public name: string;

  constructor(name: string) {
    this.name = name;
  }

  // Метод для переопределения
  greet(): string {
    return `Hello ${this.name}`;
  }
}
```

#### Дочерний класс с переопределением:

```typescript
class Admin extends User {
  // Переопределяем метод greet
  greet(): string {
    // Сигнатура та же: нет параметров, возвращает string
    return `Hello ${this.name}, I am the admin`;
  }
}
```

### 4. Что проверяет TypeScript

#### ✅ Правильное переопределение:

```typescript
class Admin extends User {
  greet(): string {
    return `Admin greeting: ${this.name}`;
  }
}
```

#### ❌ Неправильное переопределение - разные параметры:

```typescript
class Admin extends User {
  greet(prefix: string): string {
    // ❌ ОШИБКА: Разное количество параметров
    return `${prefix} ${this.name}`;
  }
}
```

#### ❌ Неправильное переопределение - другой возвращаемый тип:

```typescript
class Admin extends User {
  greet(): number {
    // ❌ ОШИБКА: Другой возвращаемый тип
    return 123;
  }
}
```

### 5. Демонстрация работы переопределения

```typescript
// Создаем экземпляры
const user = new User("John");
const admin = new Admin("Mark");

// Вызываем один и тот же метод
console.log(user.greet()); // "Hello John"
console.log(admin.greet()); // "Hello Mark, I am the admin"
```

**Что происходит:**

- `user.greet()` вызывает метод из класса `User`
- `admin.greet()` вызывает переопределенный метод из класса `Admin`

### 6. Почему важна совместимость сигнатур?

#### Принцип подстановки Барбары Лисков (LSP):

> Объекты должны быть заменяемы экземплярами их подтипов без изменения правильности программы.

```typescript
function printGreeting(user: User) {
  console.log(user.greet()); // Должно работать для ЛЮБОГО User
}

const regularUser = new User("John");
const adminUser = new Admin("Mark");

printGreeting(regularUser); // ✅ Работает
printGreeting(adminUser); // ✅ Работает - тот же метод, другое поведение
```

Если бы сигнатуры отличались, такой полиморфизм был бы невозможен.

### 7. Что можно менять при переопределении?

#### ✅ Можно менять:

- **Тело метода** - внутреннюю реализацию
- **Логику выполнения** - что именно делает метод
- **Возвращаемое значение** (но того же типа!)

#### ❌ Нельзя менять:

- **Имя метода**
- **Типы параметров**
- **Количество параметров**
- **Тип возвращаемого значения**

### 8. Расширенный пример с параметрами

Если бы метод принимал параметры:

```typescript
class User {
  greet(formal: boolean): string {
    if (formal) {
      return `Good day, ${this.name}`;
    }
    return `Hello ${this.name}`;
  }
}

class Admin extends User {
  // ✅ Правильное переопределение - та же сигнатура
  greet(formal: boolean): string {
    if (formal) {
      return `Good day, Administrator ${this.name}`;
    }
    return `Hello ${this.name}, I am the admin`;
  }
}
```

### 9. Практическое применение

#### Разные поведения для разных типов пользователей:

```typescript
class User {
  calculateDiscount(price: number): number {
    return price * 0.9; // 10% скидка для обычных пользователей
  }
}

class VIPUser extends User {
  calculateDiscount(price: number): number {
    return price * 0.7; // 30% скидка для VIP
  }
}

class Admin extends User {
  calculateDiscount(price: number): number {
    return price * 0.5; // 50% скидка для администраторов
  }
}
```

### 10. Ограничения и исключения

#### Доступ к родительской реализации:

Иногда нужно вызвать родительский метод из переопределенного:

```typescript
class Admin extends User {
  greet(): string {
    const parentGreeting = super.greet(); // Вызов родительского метода
    return `${parentGreeting}, I am the admin`;
  }
}
```

#### Модификаторы доступа:

При переопределении нельзя делать метод более закрытым:

```typescript
class User {
  public greet(): string {
    return "Hello";
  }
}

class Admin extends User {
  private greet(): string {
    // ❌ ОШИБКА: Нельзя изменить public на private
    return "Admin greeting";
  }
}
```

### Ключевые выводы:

1. **Переопределение = новая реализация** того же метода в наследнике
2. **Совместимость сигнатур обязательна** - имя, параметры, возвращаемый тип
3. **Полиморфизм в действии** - один интерфейс, разные реализации
4. **TypeScript строго проверяет** совместимость при переопределении
5. **Гибкость наследования** - можно адаптировать поведение для конкретных случаев

Переопределение методов — это мощный инструмент для создания гибких иерархий классов, где каждый уровень наследования может адаптировать поведение под свои конкретные needs! 🎯
