---

### **1. Контекст: Почему нужна перегрузка функций?**

Автор начинает с **ключевой проблемы JavaScript**:

> _"JavaScript functions are dynamic in nature. They can accept different arguments, different number of arguments, and return different results based on those arguments."_

**Пример из видео (метод `slice`):**

```javascript
const str = "hello world";
const part1 = str.slice(7); // "world" (пропускаем первые 7 символов)
const part2 = str.slice(7, 10); // "wor" (пропускаем 7, останавливаемся на 10-м символе)
```

- **Суть проблемы**: В JavaScript одна и та же функция (`slice`) ведёт себя по-разному в зависимости от количества аргументов.
- **Проблема для TypeScript**: TypeScript требует строгой типизации. Если мы попытаемся описать `slice` в TypeScript, это выглядело бы так:
  ```ts
  function slice(start: number, end?: number): string { ... }
  ```
  Но как указать, что при передаче **двух аргументов** возвращаемое значение будет **другого типа**, чем при одном аргументе? Обычные optional-параметры (`end?`) здесь не спасут — они не меняют тип возвращаемого значения динамически.

---

### **2. Задача для примера: Система бронирования авиабилетов**

Автор вводит практический пример, чтобы показать **где это нужно**:

```ts
type Reservation = {
  departureDate: Date;
  returnDate?: Date; // ? — опциональное свойство
  departingFrom: string;
  destination: string;
};
```

- **Сценарий**: Пользователь может забронировать:
  - **Двусторонний билет** (туда-обратно) → нужны `departureDate` + `returnDate`.
  - **Односторонний билет** → нужен только `departureDate`, `returnDate` отсутствует.

**Как должно выглядеть решение в идеале?**

- Функция `reserve` должна поддерживать **два режима вызова**:

  ```ts
  // Режим 1: Двусторонний билет
  reserve(
    new Date(2023, 10, 15), // departureDate
    new Date(2023, 10, 22), // returnDate
    "New York", // departingFrom
    "London" // destination
  );

  // Режим 2: Односторонний билет
  reserve(
    new Date(2023, 10, 15), // departureDate
    "New York", // departingFrom
    "London" // destination
  );
  ```

---

### **3. Синтаксис перегрузки функций (Function Overloading)**

#### **Шаг 1: Объявление перегрузок через call signatures**

Автор показывает **специальный синтаксис** для перегрузки:

```ts
type Reserve = {
  // Перегрузка 1: для двустороннего билета (4 аргумента)
  (
    departureDate: Date,
    returnDate: Date,
    departingFrom: string,
    destination: string
  ): Reservation;

  // Перегрузка 2: для одностороннего билета (3 аргумента)
  (
    departureDate: Date,
    departingFrom: string,
    destination: string
  ): Reservation;
};
```

**Ключевые детали:**

- Это **не реализация функции**, а **сигнатуры вызовов** (как «договорённости» о том, как функцию можно вызывать).
- Синтаксис похож на объект: каждая строка внутри `{}` — отдельная сигнатура вызова.
- **Запятые не нужны** между сигнатурами — это не объект JavaScript, а специальная конструкция TypeScript.
- **Порядок важен**: TypeScript проверяет перегрузки сверху вниз. Первая подходящая сигнатура будет использована.

#### **Шаг 2: Реализация функции**

Теперь создаём саму функцию, которая удовлетворяет этим сигнатурам:

```ts
const reserve: Reserve = (
  departureDate: Date,
  returnOrFrom: Date | string, // ← Union-тип!
  fromOrDestination: string,
  destination?: string // ← Optional параметр
) => {
  // ...тело функции
};
```

**Почему параметры выглядят странно?**

- TypeScript **не позволяет** иметь разные типы для одного параметра в разных перегрузках.
- Нужно найти «общий знаменатель» для всех перегрузок:
  - `returnOrFrom`:
    - В 1-й перегрузке: это `returnDate: Date`.
    - Во 2-й перегрузке: это `departingFrom: string`.
      → **Решение**: `Date | string`.
  - `fromOrDestination`:
    - В 1-й перегрузке: это `departingFrom: string`.
    - Во 2-й перегрузке: это `destination: string`.
      → **Решение**: `string` (тип совпадает).
  - `destination`:
    - Есть только в 1-й перегрузке.
      → **Решение**: сделать его опциональным (`?: string`).

#### **Шаг 3: Тело функции с runtime-проверками**

Теперь нужно **определить, какая перегрузка вызвана**, и вернуть правильный объект:

```ts
const reserve: Reserve = (...) => {
  // Проверяем: если второй аргумент — Date, и destination задан → это двусторонний билет
  if (returnOrFrom instanceof Date && destination) {
    return {
      departureDate,
      returnDate: returnOrFrom, // здесь returnOrFrom — точно Date
      departingFrom: fromOrDestination,
      destination
    };
  }
  // Иначе: если второй аргумент — string → это односторонний билет
  else if (typeof returnOrFrom === "string") {
    return {
      departureDate,
      departingFrom: returnOrFrom, // здесь returnOrFrom — точно string
      destination: fromOrDestination // fromOrDestination — destination
    };
  }
  // Если ни одно условие не подошло — ошибка
  throw new Error("Please provide valid details to reserve a ticket");
};
```

**Важные нюансы:**

- **Type guards** (`instanceof Date`, `typeof ... === "string"`) нужны, чтобы TypeScript «понял», какой тип у `returnOrFrom` в каждой ветке.
- **Optional параметр `destination`** в первой ветке проверяется как `if (destination)`, потому что он может быть `undefined`.
- **Never type в сигнатурах** (упомянуто вскользь):
  ```ts
  (): Reservation | never { ... }
  ```
  Это технический приём: если ни одна перегрузка не подошла, функция должна выкинуть ошибку (и никогда не вернуть значение). TypeScript требует явного указания `never` для таких случаев.

---

### **4. Как это работает на практике?**

Автор демонстрирует вызов:

```ts
console.log(
  reserve(
    new Date(2023, 10, 15), // departureDate
    new Date(2023, 10, 22), // returnDate (Date)
    "New York", // departingFrom
    "London" // destination
  )
);
// → { departureDate: ..., returnDate: ..., departingFrom: "New York", destination: "London" }

console.log(
  reserve(
    new Date(2023, 10, 15), // departureDate
    "New York", // departingFrom (string)
    "London" // destination
  )
);
// → { departureDate: ..., departingFrom: "New York", destination: "London" }
```

- **TypeScript автоматически сопоставляет** аргументы с правильной сигнатурой:
  - В первом случае: 4 аргумента, второй — `Date` → подходит первая сигнатура.
  - Во втором случае: 3 аргумента, второй — `string` → подходит вторая сигнатура.
- **Нет ошибок типизации**, потому что компилятор видит соответствие сигнатурам.

---

### **5. Критически важное замечание автора**

Автор специально подчёркивает:

> _"When we jump into generics, function overloading is usually not used like this. [...] This is a lot of code for nothing, but I just want to show you that something like this does exists in TypeScript."_

**Что это значит?**

- **Function overloading — инструмент «последней надежды»**. В реальном коде его используют редко, потому что:
  1. Требует дублирования логики в сигнатурах.
  2. Нужны runtime-проверки (как `instanceof`, `typeof`), которые легко сломать.
  3. Есть **более элегантные решения через generics** (но их ещё не проходили!).
- **Зачем же это показали?** Чтобы вы:
  - Понимали legacy-код (многие старые библиотеки используют перегрузки).
  - Знамли механизм для редких случаев, где generics не подходят.
  - Видели, как TypeScript решает проблему полиморфизма на уровне типов.

---

### **Итоговый вывод автора (главное!)**

- **Function overloading** — способ описать **одну функцию с несколькими режимами вызова** в TypeScript.
- **Как это работает**:
  1. Объявляем сигнатуры вызовов через объектную нотацию.
  2. Реализуем функцию, которая покрывает **все возможные комбинации** аргументов через union-типы.
  3. Используем runtime-проверки (type guards), чтобы определить, какая сигнатура вызвана.
- **Когда использовать**: крайне редко, только если нет альтернативы. В 95% случаев generics или union-типы в аргументах будут лучше.
- **Следующий шаг**: автор обещает рассказать про generics, которые решают эту же задачу **без перегрузок**.

---

### **Почему это сложно для новичков? (Мой комментарий как senior-разработчика)**

1. **Когнитивная нагрузка**: нужно держать в голове 2 уровня — сигнатуры (типы) и реализацию (код).
2. **Подводные камни**:
   - Если порядок сигнатур поменять местами, всё сломается.
   - Если забыть проверку `if (destination)` в первом условии, TypeScript не защитит от `undefined`.
3. **Исторический контекст**: перегрузки пришли из языков вроде C#/.NET, где функции строго типизированы, но нет union-типов. В TypeScript они — костыль для обратной совместимости.

> 💡 **Совет от меня**: Пока не изучите generics, запомните одно правило:  
> _"Если мне нужна перегрузка — сначала подумай, можно ли это сделать через optional-параметры (`?`) или union-типы в аргументах. Если нет — только тогда пиши перегрузки."_
