## Переопределение методов в дочерних классах

### 1. Проблема, которую решает переопределение

Автор хочет улучшить вывод праздников, добавив название отдела в заголовок:

**Было (общий заголовок):**

```
Here is the list of holidays:
```

**Стало (специфичные заголовки):**

```
Here is the list of holidays for IT Department:
Here is the list of holidays for Admin Department:
```

### 2. Что такое переопределение метода?

**Переопределение метода** — это возможность дочернего класса предоставить свою собственную реализацию метода, который уже существует в родительском классе.

**Ключевое правило:** Сигнатура метода (имя и параметры) должна оставаться неизменной.

### 3. Практическая реализация переопределения

#### Исходный метод в родительском классе:

```typescript
abstract class Department {
  public printHolidays(): void {
    if (this.holidays.length === 0) {
      console.log("There are no holidays.");
      return;
    }

    console.log("Here is the list of holidays:");
    this.holidays.forEach((holiday, index) => {
      console.log(`${index + 1}. ${holiday.reason} (${holiday.date})`);
    });
  }
}
```

#### Переопределенный метод в ITDepartment:

```typescript
class ITDepartment extends Department {
  // ... другие методы и свойства

  public printHolidays(): void {
    if (this.holidays.length === 0) {
      console.log("There are no holidays.");
      return;
    }

    console.log(`Here is the list of holidays for our ${this.name}:`);
    this.holidays.forEach((holiday, index) => {
      console.log(`${index + 1}. ${holiday.reason} (${holiday.date})`);
    });
  }
}
```

#### Переопределенный метод в AdminDepartment:

```typescript
class AdminDepartment extends Department {
  // ... другие методы и свойства

  public printHolidays(): void {
    if (this.holidays.length === 0) {
      console.log("There are no holidays.");
      return;
    }

    console.log(`Here is the list of holidays for our ${this.name}:`);
    this.holidays.forEach((holiday, index) => {
      console.log(`${index + 1}. ${holiday.reason} (${holiday.date})`);
    });
  }
}
```

### 4. Ключевое изменение - персонализированный заголовок

**Основное изменение:**

```typescript
// Было:
console.log("Here is the list of holidays:");

// Стало:
console.log(`Here is the list of holidays for our ${this.name}:`);
```

**Использование `this.name`:**

- `this.name` автоматически содержит название отдела
- Для ITDepartment: "IT Department"
- Для AdminDepartment: "Admin Department"

### 5. Как работает переопределение на практике

#### При вызове метода:

```typescript
const itDept = new ITDepartment();
const adminDept = new AdminDepartment();

itDept.printHolidays(); // Вызывает метод ITDepartment
adminDept.printHolidays(); // Вызывает метод AdminDepartment
```

#### Вывод в консоли:

```
Here is the list of holidays for our IT Department:
1. Christmas (2024-12-25T00:00:00.000Z)
2. IT Department Day (2024-03-15T00:00:00.000Z)

Here is the list of holidays for our Admin Department:
1. Christmas (2024-12-25T00:00:00.000Z)
2. Admin Department Day (2024-10-01T00:00:00.000Z)
```

### 6. Проверка соблюдения правил переопределения

#### ✅ Правильная сигнатура:

```typescript
// Родительский класс
public printHolidays(): void

// Дочерние классы
public printHolidays(): void  // ✅ Та же сигнатура
```

#### ❌ Неправильная сигнатура (ошибка):

```typescript
// Дочерний класс
public printHolidays(includeYear: boolean): void  // ❌ Другой параметр
```

### 7. Преимущества переопределения в данном сценарии

#### 1. Более информативный вывод

```typescript
// Ясно, к какому отделу относятся праздники
// Легко различать вывод разных отделов
```

#### 2. Сохранение общей логики

```typescript
// Вся основная логика (проверка пустого массива, форматирование)
// сохраняется, меняется только заголовок
```

#### 3. Гибкость для будущих изменений

```typescript
// Каждый отдел может дальше кастомизировать свой вывод
// без влияния на другие отделы
```

### 8. Проблема дублирования кода

Автор честно указывает на проблему:

**Дублирование:**

- Один и тот же код скопирован в два класса
- Нарушение принципа DRY (Don't Repeat Yourself)

**Текущее состояние:**

```typescript
// Один и тот же код в ITDepartment и AdminDepartment
public printHolidays(): void {
    // Идентичная реализация, кроме одной строки
}
```

### 9. Визуализация архитектуры после переопределения

```
Abstract Class: Department
    ├── printHolidays() ❌ (больше не используется для IT и Admin)
    ├── ITDepartment
    │   └── printHolidays() ✅ (переопределен)
    └── AdminDepartment
        └── printHolidays() ✅ (переопределен)
```

### 10. Практическая демонстрация полиморфизма

#### Даже с переопределением сохраняется полиморфизм:

```typescript
const departments: Department[] = [new ITDepartment(), new AdminDepartment()];

departments.forEach((dept) => {
  dept.printHolidays(); // Вызывает правильную версию метода
});
```

**Результат:**

- Для ITDepartment: выводит с заголовком IT Department
- Для AdminDepartment: выводит с заголовком Admin Department

### 11. Сравнение до и после переопределения

#### До переопределения:

```typescript
// Все отделы используют один метод
// Заголовок общий для всех
```

#### После переопределения:

```typescript
// Каждый отдел использует свой метод
// Заголовок специфичен для каждого отдела
```

### 12. Ограничения текущего подхода

#### Проблемы:

1. **Дублирование кода** - одинаковый код в двух местах
2. **Сложность поддержки** - изменения нужно вносить в нескольких классах
3. **Нарушение DRY** - принцип "Не повторяйся" нарушен

#### Решение (обещано в следующем видео):

- Убрать дублирование
- Сохранить преимущества переопределения

### 13. Мысленный процесс проектирования

**Вопросы при переопределении:**

1. "Что должно отличаться в поведении дочерних классов?"
2. "Что должно остаться общим?"
3. "Как избежать дублирования кода?"

### 14. Практические сценарии для переопределения

#### Другие возможные улучшения:

```typescript
// Разное форматирование дат для разных отделов
class ITDepartment extends Department {
  public printHolidays(): void {
    // IT отдел предпочитает ISO формат
    console.log(`IT Department Holidays (ISO format):`);
    this.holidays.forEach((holiday, index) => {
      console.log(
        `${index + 1}. ${holiday.reason} (${holiday.date.toISOString()})`
      );
    });
  }
}

class AdminDepartment extends Department {
  public printHolidays(): void {
    // Админ отдел предпочитает локализованный формат
    console.log(`Admin Department Holidays (Local format):`);
    this.holidays.forEach((holiday, index) => {
      const localDate = holiday.date.toLocaleDateString();
      console.log(`${index + 1}. ${holiday.reason} (${localDate})`);
    });
  }
}
```

### Ключевые выводы:

1. **Переопределение методов** - изменение реализации при сохранении сигнатуры
2. **Персонализация поведения** - каждый класс может адаптировать метод под свои needs
3. **Соблюдение контракта** - имя и параметры метода должны оставаться неизменными
4. **Проблема дублирования** - копирование кода приводит к maintenance issues
5. **Подготовка к рефакторингу** - автор обещает улучшить архитектуру в следующем видео

Переопределение методов — это мощный инструмент для создания гибких иерархий классов, где общее поведение может быть адаптировано для конкретных случаев! 🎯
