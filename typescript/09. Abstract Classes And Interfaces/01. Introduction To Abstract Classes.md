### 🔑 **Ключевые понятия из урока (по автору):**

Автор начинает введение в **абстрактные классы (abstract classes)** в TypeScript. Основная цель этого урока — показать:

1. Что такое абстрактный класс и чем он отличается от обычного.
2. Как работают **абстрактные члены класса** (свойства/методы).
3. Почему их использование **принудительно** для дочерних классов.
4. Практический пример на основе иерархии классов `Department`.

---

### 📌 **Пошаговое объяснение примера автора:**

#### 1. **Создание базового класса `Department`**

- Автор объявляет класс `Department` как **родительский класс** для будущих отделов (IT, Admin и др.).

```typescript
class Department {
  constructor(protected name: string) {}
}
```

- **Важные детали:**
  - Свойство `name` помечено как `protected`, чтобы дочерние классы могли к нему обращаться.
  - Это ещё **не абстрактный класс** — пока это обычный класс.

#### 2. **Преобразование класса в абстрактный**

- Автор добавляет ключевое слово `abstract` перед объявлением класса:

```typescript
abstract class Department {
  constructor(protected name: string) {}
}
```

- **Главное правило абстрактных классов (по автору):**
  > _"Abstract classes cannot be instantiated. You cannot use the `new` keyword with an abstract class."_  
  > _(Абстрактные классы нельзя инстанцировать. Нельзя использовать `new` с абстрактным классом.)_
- **Демонстрация ошибки TypeScript:**
  ```typescript
  const dept = new Department("Test"); // Ошибка: "Cannot create an instance of an abstract class"
  ```
- **Почему это важно?**  
  Цель абстрактного класса — быть **только базой для наследования**, а не созданием объектов.

#### 3. **Оптимизация: `protected` конструктор**

- Так как `Department` нельзя инстанцировать, публичный конструктор (`public`) бессмысленен. Автор делает его `protected`:

```typescript
abstract class Department {
  protected constructor(protected name: string) {}
}
```

- **Зачем это нужно?**  
  Дочерние классы смогут вызывать этот конструктор при наследовании, но прямое создание экземпляра `Department` станет невозможным даже без ошибки TypeScript (на уровне доступа).

#### 4. **Создание дочерних классов**

- Автор создаёт два класса, наследующих `Department`:

```typescript
class ITDepartment extends Department {}
class AdminDepartment extends Department {}
```

- Пока в них нет логики — они просто наследуют `name` и `protected` конструктор.

#### 5. **Введение абстрактного свойства `holidays`**

- **Задача:**  
  Каждый отдел (IT, Admin) должен иметь **свой уникальный список праздников**.  
  _Пример автора:_
  > _"Holidays of admin department will be different from the holidays of IT department."_
- **Решение:**  
  Создать **абстрактное свойство** в родительском классе, которое **обязательно** должно быть реализовано в каждом дочернем классе.
- **Шаг 1: Определение типа `Holidays`**  
  Автор создаёт кастомный тип для структуры данных:

  ```typescript
  type Holidays = {
    date: Date;
    reason: string;
  }[];
  ```

  Это массив объектов, где каждый объект содержит дату (`Date`) и причину праздника (`string`).

- **Шаг 2: Объявление абстрактного свойства**  
  В классе `Department` добавляется:

  ```typescript
  abstract protected holidays: Holidays;
  ```

  Ключевые слова здесь:

  - `abstract` — свойство не имеет реализации в родительском классе.
  - `protected` — доступно только внутри класса и его дочерних классов.

- **Что происходит при объявлении?**  
  TypeScript сразу показывает ошибку в дочерних классах:

  > _"ITDepartment does not implement inherited abstract member 'holidays' from class 'Department'."_  
  > _(ITDepartment не реализует унаследованный абстрактный член 'holidays' из класса 'Department'.)_

- **Суть абстрактных членов (по автору):**
  > _"Once an abstract member is declared in the parent class, the child class must implement its own functionality of the abstract members."_  
  > _(Как только абстрактный член объявлен в родительском классе, дочерний класс обязан реализовать его функционал.)_  
  > → Это гарантирует, что **каждый отдел** будет иметь своё свойство `holidays`.

#### 6. **Реализация абстрактного свойства в дочерних классах**

- Автор добавляет свойство `holidays` в оба дочерних класса:

```typescript
class ITDepartment extends Department {
  protected holidays: Holidays = [];
}

class AdminDepartment extends Department {
  protected holidays: Holidays = [];
}
```

- **Важные нюансы:**
  - Значение по умолчанию — пустой массив (`[]`). Автор поясняет:
    > _"For now, we can just assign an empty array as the value to the holidays, as later on we will be writing methods to add and retrieve holidays."_  
    > _(Пока присваиваем пустой массив, потому что позже добавим методы для работы с праздниками.)_
  - TypeScript перестаёт ругаться на отсутствие `holidays`, так как условие абстрактного класса выполнено.

---

### 💡 **Итоги урока (ключевые выводы автора):**

1. **Абстрактные классы (`abstract class`)**  
   — Используются **только как база для наследования**.  
   — **Нельзя создавать экземпляры** через `new`.  
   — Конструктор обычно помечается как `protected`.

2. **Абстрактные члены (`abstract` свойства/методы)**  
   — Объявляются в родительском классе **без реализации** (только сигнатура).  
   — **Обязательны к реализации** во всех дочерних классах.  
   — Позволяют **гарантировать наличие определённой функциональности** в иерархии классов.

3. **Практическая польза (пример с `holidays`)**  
   — Каждый отдел (IT, Admin) может иметь **свою уникальную логику** для праздников.  
   — TypeScript **принудительно проверяет** наличие свойства `holidays` в дочерних классах.  
   — Это **защищает от ошибок**, когда разработчик забывает добавить обязательное свойство.

---

### ⚠️ **Что автор НЕ делал в этом уроке (важно для вашего запроса):**

- Не сравнивал абстрактные классы с интерфейсами (`interface`).
- Не показывал абстрактные методы (только свойства).
- Не рассматривал множественное наследование или другие ООП-концепции.
- Не использовал реальные данные для `holidays` (только пустой массив как заглушка).
- Не обсуждал модификаторы доступа кроме `protected`.

---

### 📚 **Фразы автора, которые стоит запомнить:**

> _"The whole purpose of creating an abstract class is to create child classes from an abstract class."_  
> _"Abstract members in the parent class are not concrete implementations. They exist so that their implementations can be forced inside the child classes."_  
> _"Whenever a developer would try to create a new department from the parent department class, TypeScript will force the developer to implement the holidays property."_

---

Это **полный и исчерпывающий разбор именно того, что автор рассказывает в этом видео**. В следующем уроке, как он анонсировал, будут разбираться **абстрактные методы** — но это уже другая тема. Если нужно углубиться в детали текущего урока — задавайте конкретные вопросы! 😊
