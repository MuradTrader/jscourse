## 1. Вступление и переход к практическому применению

Автор начинает с напоминания: в предыдущем видео мы создали два декоратора, которые были **пассивными** - они только выводили информацию в консоль, но ничего не делали.

**Ключевая мысль:** Сейчас мы переходим от **наблюдения** к **модификации**. Декораторы могут реально изменять поведение кода.

## 2. Фокусировка на декораторе методов

Автор говорит: "Let's get rid of this class decorator. We're going to work on class decorators separately..."

**Объяснение:** Он сознательно убирает декоратор класса, чтобы сфокусироваться только на декораторе методов. Это педагогический приём - изучаем одну концепцию за раз.

## 3. Основная идея модификации методов

Автор объясняет фундаментальную концепцию:

```typescript
function methodLogger(target: any, context: any) {
  // Мы получаем оригинальный метод как параметр target
  // Раз мы имеем доступ к оригинальному методу...
  // ...мы можем его ИЗМЕНИТЬ или ЗАМЕНИТЬ
}
```

**Разжёвываем эту мысль:**

- `target` - это **ссылка** на оригинальный метод
- Если у вас есть ссылка на функцию, вы можете:
  - Вызвать её
  - Модифицировать её поведение
  - Полностью заменить её другой функцией

## 4. Создание функции-замены - ДЕТАЛЬНО

Вот как автор это делает, с подробным объяснением каждого шага:

```typescript
function methodLogger(target: any, context: any) {
  // Шаг 1: Создаём новую функцию, которая ЗАМЕНИТ оригинальный метод
  function replacementMethod(this: any, ...args: any[]) {
    // Эта функция станет НОВЫМ методом greet
  }

  // Шаг 2: Возвращаем эту функцию
  return replacementMethod;
}
```

**Почему это работает?**

- TypeScript "понимает", что если декоратор метода **возвращает функцию**, то эта функция должна заменить оригинальный метод
- Это **соглашение** в системе декораторов TypeScript

## 5. Подробный разбор параметров функции-замены

Автор объясняет КАЖДЫЙ параметр:

### Первый параметр: `this: any`

```typescript
function replacementMethod(this: any, ...args: any[]) {
```

- **`this`** - это специальный параметр в TypeScript, который указывает на контекст выполнения
- Когда метод вызывается как `user.greet()`, `this` внутри метода указывает на объект `user`
- В декораторе мы должны явно указать `this`, чтобы сохранить правильный контекст

### Второй параметр: `...args: any[]`

```typescript
function replacementMethod(this: any, ...args: any[]) {
```

- **`...args`** - это оператор "rest" (остаточные параметры)
- Он собирает **ВСЕ** аргументы, переданные в метод, в массив
- Если метод вызывается как `user.greet("Hello", 123)`, то `args` будет `["Hello", 123]`

## 6. Критически важный момент - ВОЗВРАТ функции

Автор специально показывает ошибку, чтобы подчеркнуть важность:

```typescript
function methodLogger(target: any, context: any) {
  function replacementMethod(this: any, ...args: any[]) {
    console.log("Args:", args);
    console.log("This:", this);
  }

  // ЗАБЫЛИ ВЕРНУТЬ ФУНКЦИЮ!
  // return replacementMethod; ← ЭТОЙ СТРОКИ НЕТ
}
```

**Что происходит без return:**

- Декоратор выполняется при компиляции
- Создаётся функция `replacementMethod`
- Но так как она не возвращается, TypeScript её игнорирует
- Оригинальный метод `greet` остаётся неизменным

## 7. Время выполнения - РАЗДЕЛЕНИЕ понятий

Автор специально добавляет console.log, чтобы показать разницу:

```typescript
function methodLogger(target: any, context: any) {
  console.log("🔧 Декоратор выполняется ПРИ КОМПИЛЯЦИИ");

  return function replacementMethod(this: any, ...args: any[]) {
    console.log("🚀 Функция-замена выполняется ПРИ ВЫЗОВЕ МЕТОДА");
  };
}
```

**Разница:**

- **Декоратор** выполняется, когда TypeScript компилируется в JavaScript (один раз)
- **Функция-замена** выполняется каждый раз, когда вызывается метод (много раз)

## 8. Практический пример: логирование времени выполнения

Теперь автор показывает РЕАЛЬНОЕ применение:

```typescript
function methodLogger(originalMethod: any, context: any) {
  return function replacementMethod(this: any, ...args: any[]) {
    // Шаг 1: Логируем начало выполнения
    console.log("Method invocation started");

    // Шаг 2: ВЫЗЫВАЕМ оригинальный метод
    const result = originalMethod.call(this, ...args);

    // Шаг 3: Логируем конец выполнения
    console.log("Method invocation ended");

    return result;
  };
}
```

## 9. Самая важная часть: КАК правильно вызвать оригинальный метод

Автор очень подробно объясняет эту строку:

```typescript
const result = originalMethod.call(this, ...args);
```

**Разбираем по частям:**

### `originalMethod.call()`

- `.call()` - это встроенный метод JavaScript у всех функций
- Он позволяет вызвать функцию с указанным значением `this`

### Первый аргумент: `this`

- Мы передаём `this` из функции-замены
- Это гарантирует, что оригинальный метод будет вызван в правильном контексте
- Без этого `this` внутри оригинального метода был бы `undefined` или глобальный объект

### Второй аргумент: `...args`

- Мы "разворачиваем" массив `args` обратно в отдельные аргументы
- Если `args = ["Hello", 123]`, то `...args` становится `"Hello", 123`
- Так мы передаём все оригинальные аргументы в оригинальный метод

## 10. Демонстрация с аргументами метода

Автор добавляет параметр в метод `greet`, чтобы показать, как работают аргументы:

```typescript
class Person {
  @methodLogger
  greet(greeting: string) {
    // ← теперь есть параметр
    console.log(`${greeting}, my name is ${this.name}`);
  }
}

// При вызове:
user.greet("Hello");
```

**Что происходит в функции-замене:**

- `args = ["Hello"]` (массив с одним элементом)
- При вызове `originalMethod.call(this, ...args)`:
  - `...args` разворачивается в `"Hello"`
  - Вызов становится: `originalMethod.call(this, "Hello")`
  - Оригинальный метод получает свой параметр `greeting = "Hello"`

## 11. Полная последовательность выполнения

Давайте проследим ВСЕ шаги при вызове `user.greet("Hello")`:

1. **Вызывается функция-замена** (так как декоратор её вернул)
2. **Выполняется** `console.log('Method invocation started')`
3. **Вызывается** `originalMethod.call(this, "Hello")`
   - `this` = объект `user` (контекст вызова)
   - `"Hello"` передаётся как параметр
4. **Выполняется оригинальный метод** `greet`:
   - `console.log("Hello, my name is John")`
5. **Выполняется** `console.log('Method invocation ended')`

## 12. Итоговый код с комментариями для каждого шага

```typescript
// ДЕКОРАТОР - выполняется при КОМПИЛЯЦИИ
function methodLogger(originalMethod: any, context: any) {
  console.log("🎯 Декоратор применён к методу:", context.name);

  // ФУНКЦИЯ-ЗАМЕНА - выполняется при КАЖДОМ ВЫЗОВЕ метода
  return function replacementMethod(this: any, ...args: any[]) {
    // Шаг 1: Логируем начало
    console.log("⏰ Вызов метода начался");

    // Шаг 2: Вызываем оригинальный метод с правильным контекстом и аргументами
    const result = originalMethod.call(this, ...args);

    // Шаг 3: Логируем конец
    console.log("✅ Вызов метода завершился");

    // Возвращаем результат оригинального метода
    return result;
  };
}

class Person {
  constructor(public name: string) {}

  @methodLogger
  greet(greeting: string) {
    console.log(`${greeting}, my name is ${this.name}`);
  }
}

// ИСПОЛЬЗОВАНИЕ:
const user = new Person("John");
user.greet("Hello");

// ВЫВОД в консоль:
// 🎯 Декоратор применён к методу: greet        (при компиляции)
// ⏰ Вызов метода начался                      (при вызове)
// Hello, my name is John                      (оригинальный метод)
// ✅ Вызов метода завершился                   (при вызове)
```
