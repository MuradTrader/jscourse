Отлично! Давай разберём этот отрывок максимально подробно, как будто я — профессиональный TypeScript-разработчик и твой наставник. Я объясню всё по шагам, включая **зачем** делается каждое действие, **как оно работает под капотом**, и **почему это важно** для тебя как для разработчика.

---

### 🔧 **1. Установка TypeScript глобально**

Автор начинает с установки TypeScript **глобально** на компьютер с помощью команды:

```bash
npm install -g typescript
```

#### Что это значит?

- `npm` — менеджер пакетов Node.js.
- `-g` (или `--global`) означает, что пакет устанавливается **не в текущий проект**, а **на весь компьютер**.
- После этого в терминале становится доступна команда `tsc` (TypeScript Compiler) **из любого места**.

#### Зачем это делается?

- Чтобы можно было **вручную скомпилировать любой `.ts` файл в `.js`**, даже вне проекта.
- Это удобно для **быстрого тестирования** или обучения.

> 💡 **Важно**: Глобальная установка — это **временное решение для обучения**. В реальных проектах TypeScript почти всегда устанавливается **локально** (об этом позже).

---

### 📄 **2. Создание первого файла TypeScript**

Автор создаёт файл `index.ts` и пишет в нём обычный JavaScript:

```ts
console.log("Hello World");
```

#### Почему это работает?

- **TypeScript — надмножество JavaScript**. Любой валидный JS-код — это автоматически валидный TS-код.
- Расширение `.ts` говорит системе: «Это файл TypeScript, его нужно скомпилировать».

---

### ⚙️ **3. Компиляция через `tsc`**

Команда:

```bash
tsc index.ts
```

создаёт файл `index.js`.

#### Что происходит?

- Компилятор TypeScript (`tsc`) читает `index.ts`.
- Проверяет типы (в данном случае их нет — просто JS).
- Генерирует **эквивалентный JavaScript-код** в `index.js`.
- Этот `.js` файл можно запускать в **Node.js** или в **браузере**.

> 🔍 **Node.js** используется здесь просто потому, что это самый быстрый способ запустить JS без HTML-страницы.

---

### 🚫 **4. Опасность `npx tsc`**

Автор предупреждает: **не используйте `npx tsc` без понимания последствий**.

Команда:

```bash
npx tsc index.ts
```

#### В чём разница?

| `tsc` (глобальный)                                               | `npx tsc`                                                |
| ---------------------------------------------------------------- | -------------------------------------------------------- |
| Использует **локально установленную** (или глобальную) версию TS | Всегда скачивает и использует **последнюю версию** с npm |
| Уважает ваш **локальный `tsconfig.json`**                        | **Игнорирует** ваш `tsconfig.json`!                      |
| Предсказуемое поведение                                          | Может сломать сборку из-за неожиданной версии TS         |

> ❗ Это частая ошибка новичков: они думают, что `npx tsc` — «более современный» способ, но на самом деле он **обходит ваши настройки**.

---

### 📁 **5. Создание `tsconfig.json`**

Команда:

```bash
tsc --init
```

создаёт файл `tsconfig.json` — **главный конфигурационный файл TypeScript**.

#### Зачем он нужен?

- Указывает компилятору, **как именно** нужно транспилировать код.
- Например:
  - В какую версию JS компилировать (`ES5`, `ES2020`, и т.д.)
  - Где искать файлы
  - Нужны ли декларации типов
  - Включать ли строгую проверку типов и т.п.

#### Важное поведение:

После создания `tsconfig.json` команда просто:

```bash
tsc
```

автоматически:

- Находит **все `.ts` файлы** в проекте (или в папках, указанных в конфиге)
- Компилирует их **все сразу**

> ✅ Это экономит время: не нужно указывать каждый файл вручную.

---

### 👀 **6. Режим наблюдения (`--watch` или `-w`)**

Команда:

```bash
tsc -w
```

включает **режим слежения**.

#### Как это работает?

- Компилятор **не завершает работу**, а остаётся в памяти.
- Следит за **изменениями всех `.ts` файлов**.
- При любом сохранении (`Ctrl+S`) — **автоматически перекомпилирует** нужные файлы.

> 💡 Это критически важно при разработке: ты пишешь код → сохраняешь → сразу видишь результат без ручного запуска `tsc`.

---

### 📦 **7. Переход к локальной установке TypeScript**

Автор создаёт `package.json` через:

```bash
npm init
```

и затем устанавливает TypeScript **локально** как dev-зависимость:

```bash
npm install -D typescript
```

#### Почему это лучше?

| Глобальная установка                         | Локальная установка                                                    |
| -------------------------------------------- | ---------------------------------------------------------------------- |
| Одна версия TS на весь компьютер             | Каждый проект — своя версия TS                                         |
| Риск несовместимости между проектами         | Полный контроль над версией                                            |
| Нельзя зафиксировать версию в `package.json` | Версия записана в `package.json` → все в команде используют одинаковую |

> 🛠️ TypeScript **нужен только на этапе разработки** — в production идёт уже скомпилированный JS. Поэтому `-D` (devDependency).

---

### ▶️ **8. Создание npm-скрипта для запуска в watch-режиме**

В `package.json` добавляется:

```json
{
  "scripts": {
    "start:dev": "tsc -w"
  }
}
```

Теперь можно запускать:

```bash
npm run start:dev
```

#### Преимущества:

- Не нужно запоминать команды.
- Все разработчики проекта используют **один и тот же workflow**.
- Легко интегрировать в CI/CD, IDE и т.д.

> ✅ Это **стандартная практика** в современной разработке.

---

### 🧠 **Ключевые выводы из этого урока**

1. **TypeScript = JavaScript + типы** → любой JS-код — валидный TS.
2. **Глобальная установка** — для быстрого старта, но **локальная — для реальных проектов**.
3. **`tsconfig.json` — сердце проекта на TS**. Без него компилятор работает с настройками по умолчанию.
4. **Watch-режим (`-w`)** — must-have при разработке.
5. **`npx tsc` опасен**, если не понимаешь, что он игнорирует твой `tsconfig.json`.
6. **TypeScript не нужен в production** — он только помогает писать безопасный код на этапе разработки.

---
