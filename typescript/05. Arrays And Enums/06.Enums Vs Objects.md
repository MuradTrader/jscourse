## Общее введение

Автор сравнивает **объекты** и **enum** в TypeScript, показывая, как они могут использоваться для схожих целей, но с важными отличиями в поведении при компиляции и использовании.

---

## 1. Const Enum vs Regular Enum

### Regular Enum (обычный enum):

```typescript
enum Direction {
  Up,
  Down,
  Left,
  Right,
}
```

### Const Enum (const enum):

```typescript
const enum EDirection {
  Up,
  Down,
  Left,
  Right,
}
```

---

## 2. Ключевое отличие в компиляции

### Regular Enum компилируется в JavaScript объект:

```typescript
// TypeScript
enum Direction {
  Up,
  Down,
}

// JavaScript после компиляции
var Direction;
(function (Direction) {
  Direction[(Direction["Up"] = 0)] = "Up";
  Direction[(Direction["Down"] = 1)] = "Down";
})(Direction || (Direction = {}));
```

### Как работает код JS после компляции

Этот код создаёт **числовой перечисляемый тип (enum)** в JavaScript под названием `Direction`. Хотя JavaScript сам по себе не имеет встроенной поддержки `enum` (в отличие от TypeScript), такой синтаксис — это то, как **TypeScript компилирует enum в обычный JavaScript**.

Разберём по шагам, что делает этот код:

---

### 1. Объявление переменной

```js
var Direction;
```

Создаётся переменная `Direction`, которая изначально равна `undefined`.

---

### 2. Самовызывающаяся функция (IIFE)

```js
(function (Direction) {
  // тело функции
})(Direction || (Direction = {}));
```

Это функция, которая сразу же вызывается. В неё передаётся текущее значение `Direction`.  
Если `Direction` ещё не существует (то есть `undefined`), то создаётся пустой объект: `Direction = {}`.

Таким образом, внутри функции параметр `Direction` ссылается на объект (либо существующий, либо новый пустой).

---

### 3. Добавление свойств в объект

Внутри функции выполняются такие строки:

```js
Direction[(Direction["Up"] = 0)] = "Up";
Direction[(Direction["Down"] = 1)] = "Down";
```

Разберём первую подробнее:

#### a. `Direction["Up"] = 0`

— присваивает свойству `"Up"` значение `0`.  
Теперь объект выглядит так:

```js
{
  Up: 0;
}
```

#### b. Затем используется это значение (`0`) как **ключ** для нового присваивания:

`Direction[0] = "Up"`

Теперь объект выглядит так:

```js
{
  Up: 0,
  0: "Up"
}
```

Аналогично для `"Down"`:

- `Direction["Down"] = 1` → добавляет `Down: 1`
- `Direction[1] = "Down"` → добавляет `1: "Down"`

Итоговый объект:

```js
{
  Up: 0,
  Down: 1,
  0: "Up",
  1: "Down"
}
```

---

### Зачем это нужно?

Такой подход позволяет использовать enum **в обе стороны**:

- По имени получить число: `Direction.Up` → `0`
- По числу получить имя: `Direction[0]` → `"Up"`

Это удобно для:

- Читаемости кода (`if (direction === Direction.Up)`)
- Отладки (в логах видно число, но можно легко понять, что оно означает)

---

### Важно:

Этот код — результат **компиляции TypeScript-кода**:

```ts
enum Direction {
  Up,
  Down,
}
```

В чистом JavaScript программисты редко пишут такие конструкции вручную — чаще используют простые объекты или `Object.freeze`.

---

Если кратко:

> Этот код создаёт двунаправленное соответствие между именами (`"Up"`, `"Down"`) и числами (`0`, `1`) внутри объекта `Direction`.

---

### 🔑 Ключевой момент:

В JavaScript **оператор присваивания (`=`) сам по себе возвращает значение**, которое было присвоено.

То есть выражение:

```js
Direction["Up"] = 0;
```

**не только** записывает `0` в свойство `"Up"` объекта `Direction`,  
**но и возвращает** значение `0`.

👉 Это поведение — часть языка. Присваивание — это **выражение**, а не только **инструкция**.

---

### 🧪 Пример для ясности:

```js
let x;
let y = (x = 5);
console.log(x); // 5
console.log(y); // 5
```

Здесь `x = 5` присваивает `5` переменной `x`, **и возвращает `5`**, которое затем присваивается `y`.

То же самое происходит в:

```js
Direction[(Direction["Up"] = 0)] = "Up";
```

Разберём изнутри наружу:

1. **Выполняется**: `Direction["Up"] = 0`

   - Объекту `Direction` добавляется свойство: `Direction.Up = 0`
   - **Результат этого выражения — `0`**

2. **Теперь внешнее выражение** становится:

   ```js
   Direction[0] = "Up";
   ```

   - Потому что вместо `Direction["Up"] = 0` подставляется его результат — `0`

3. Итог: у объекта появляются два свойства:
   - `Direction.Up` → `0`
   - `Direction[0]` → `"Up"`

---

### 💡 Почему так сделано в enum?

Это позволяет использовать перечисление **в обе стороны**:

```js
console.log(Direction.Up); // 0
console.log(Direction[0]); // "Up"
```

Без такого «хака» с возвратом значения присваивания, пришлось бы писать вручную две строки:

```js
Direction.Up = 0;
Direction[0] = "Up";
```

А благодаря тому, что `a = b` возвращает `b`, можно сделать это **в одной строке**.

---

### ✅ Вывод:

> Выражение `Direction["Up"] = 0` возвращает `0`, потому что **в JavaScript оператор присваивания возвращает присвоенное значение**. Это стандартное поведение языка и используется здесь для краткой инициализации двунаправленного enum.

### Const Enum НЕ компилируется в объект:

```typescript
// TypeScript
const enum EDirection {
  Up,
  Down,
}
let direction = EDirection.Up;

// JavaScript после компиляции
let direction = 0; // Просто значение, без объекта
```

---

## 3. Практическая демонстрация компиляции

### Исходный TypeScript код:

```typescript
const enum EDirection {
  Up,
  Down,
  Left,
  Right,
}

enum Direction {
  Up,
  Down,
  Left,
  Right,
}

// Использование значений
let eDirection: EDirection = EDirection.Up;
let direction: Direction = Direction.Right;
```

### Скомпилированный JavaScript:

```javascript
// EDirection полностью исчез - нет объекта
let eDirection = 0; // Просто значение

// Direction остался как объект
var Direction;
(function (Direction) {
  Direction[(Direction["Up"] = 0)] = "Up";
  Direction[(Direction["Down"] = 1)] = "Down";
  Direction[(Direction["Left"] = 2)] = "Left";
  Direction[(Direction["Right"] = 3)] = "Right";
})(Direction || (Direction = {}));

let direction = Direction.Right; // Обращение к объекту
```

---

## 4. Объекты как альтернатива enum

### Объект с `as const` утверждением:

```typescript
const ODirection = {
  Up: 0,
  Down: 1,
  Left: 2,
  Right: 3,
} as const;
```

### Что делает `as const`:

- Делает все свойства **readonly**
- Делает все значения **литеральными типами**
- Запрещает изменение объекта

### Результат типа:

```typescript
// Тип ODirection после as const:
{
    readonly Up: 0;
    readonly Down: 1;
    readonly Left: 2;
    readonly Right: 3;
}
```

---

## 5. Сравнение поведения объектов и enum

### Попытка изменения (для всех случаев ❌ ОШИБКА):

**Regular Enum:**

```typescript
Direction.Up = 5; // ❌ Cannot assign to 'Up' because it is a read-only property
```

**Const Enum:**

```typescript
EDirection.Up = 5; // ❌ Cannot assign to 'Up' because it is a read-only property
```

**Object with as const:**

```typescript
ODirection.Up = 5; // ❌ Cannot assign to 'Up' because it is a read-only property
```

---

## 6. Преимущества и недостатки каждого подхода

### Regular Enum:

**✅ Преимущества:**

- Runtime объект доступен для интроспекции
- Можно перебирать ключи/значения
- Обратное mapping (из значения в имя)

**❌ Недостатки:**

- Добавляет код в бандл
- Overhead в runtime

### Const Enum:

**✅ Преимущества:**

- Нет runtime overhead
- Значения inline (лучшая производительность)
- Не добавляет код в бандл

**❌ Недостатки:**

- Недоступен в runtime
- Нет интроспекции
- Требует TypeScript для компиляции

### Object with as const:

**✅ Преимущества:**

- Простой JavaScript синтаксис
- Runtime объект доступен
- Более гибкий (можно добавлять методы)

**❌ Недостатки:**

- Нет обратного mapping
- Нет автоматической нумерации
- Более многословный

---

## 7. Рекомендации автора

### Предпочтение Enum:

> "Я предпочитаю объявлять enum, потому что это очень явно и намерение понятно"

### Причины выбора enum:

- **Явность** - четко показывает намерение создать перечисление
- **Стандартизация** - общепринятый подход в TypeScript
- **Богатый функционал** - обратное mapping, автоматическая нумерация

### Когда использовать объекты:

- В современном TypeScript коде как константы
- Когда нужна дополнительная гибкость
- Для совместимости с JavaScript библиотеками

---

## 8. Практические сценарии использования

### Enum идеально для:

```typescript
// Статусы, роли, направления - фиксированные наборы
enum OrderStatus {
  Pending,
  Processing,
  Shipped,
  Delivered,
}

enum UserRole {
  Admin = "admin",
  User = "user",
  Guest = "guest",
}
```

### Объекты с as const для:

```typescript
// Конфигурации, настройки, флаги
const AppConfig = {
  Theme: "dark",
  Language: "en",
  Version: "1.0.0",
} as const;

// Комбинации значений
const Permissions = {
  Read: 1,
  Write: 2,
  Execute: 4,
} as const;
```

---

## 9. Производительность и размер бандла

### Const Enum (самый эффективный):

```typescript
const enum Size {
  Small,
  Medium,
  Large,
}
let mySize = Size.Small;
// Компилируется в: let mySize = 0;
```

### Object with as const (средняя эффективность):

```typescript
const Size = { Small: 0, Medium: 1, Large: 2 } as const;
let mySize = Size.Small;
// Остается как объект, но значения известны на этапе компиляции
```

### Regular Enum (наименее эффективный):

```typescript
enum Size {
  Small,
  Medium,
  Large,
}
let mySize = Size.Small;
// Создает runtime объект
```

---

## Итог от автора

Автор подчеркивает:

1. **Regular Enum** компилируется в JavaScript объекты
2. **Const Enum** не компилируется в объекты, значения inline
3. **Объекты с as const** становятся read-only и могут имитировать enum
4. **Enum более явные** для выражения намерения перечисления
5. **Выбор зависит** от потребностей в runtime и размера бандла

### Мой итог как разработчика

Вы изучили важные нюансы выбора между разными подходами к созданию констант:

1. **Критерии выбора:**

   **Используйте Regular Enum когда:**

   - Нужен runtime доступ к enum как объекту
   - Требуется обратное mapping (значение → имя)
   - Хотите перебирать значения

   **Используйте Const Enum когда:**

   - Критичен размер бандла
   - Нужна максимальная производительность
   - Runtime доступ к enum не требуется

   **Используйте Object with as const когда:**

   - Нужна гибкость (методы, вычисляемые свойства)
   - Работаете в mixed TypeScript/JavaScript среде
   - Предпочитаете JavaScript-подобный синтаксис

2. **Современные тенденции:**

   ```typescript
   // Современный TypeScript часто использует оба подхода

   // Для внутренних перечислений - const enum
   const enum InternalStatus {
     Active,
     Inactive,
   }

   // Для API/публичных контрактов - объекты с as const
   export const ApiEndpoints = {
     Users: "/api/users",
     Posts: "/api/posts",
   } as const;
   ```

3. **Производительность:**

   - **Const enum** → лучшая производительность, нет runtime
   - **Object with as const** → хороший баланс
   - **Regular enum** → больше памяти, но больше возможностей

4. **Совместимость:**
   - **Enum** - только TypeScript
   - **Objects** - работают в чистом JavaScript

**Резюме:** Выбор между enum и объектами зависит от конкретных требований проекта. Enum предоставляют более богатый функционал и явно выражают намерение, в то время как объекты с `as const` предлагают большую гибкость и JavaScript-совместимость. Const enum - это инструмент для оптимизации, когда runtime доступ не нужен.
